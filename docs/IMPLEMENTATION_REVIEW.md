# 전체 구현 검토

## 2026-09-25 게시 전 독립 확인

R14/R15 수정과 회귀를 확인했다. 빌드/typecheck/lint, OS 전용을 포함한 Vitest 109개, Edge E2E 1개, 임시 서비스 재시작 시험이 통과했다. R13은 게시 전 첫 시험에서도 5초 timeout으로 재현되어 해당 통합 시험의 상한만 15초로 조정했고 전체 재실행이 통과했다. 아래 미해결 표시는 각 수정 전 이력으로 읽는다. 제품 코드의 추가 수정 없이 시험 제한과 게시 제외 규칙 및 검증 문서를 정리했다. 외부 모델/클라이언트·운영 부하와 기존 장애 주입 미검증은 별도다.

## 2026-09-25 R15 수정 결과

R15 잠금 생성 경합을 수정하고 임시 데이터 회귀를 통과했다. `AsyncDpapiSecretStore`는 다른 작성자의 잠금 내용을 읽고 삭제하지 않는다. 빈 파일·부분 JSON·종료된 PID의 잠금은 획득 제한시간 10초까지 기다린 뒤 `SECRET_STORE_LOCKED`로 실패한다. 본인 잠금은 정확한 토큰 일치 시에만 해제한다. 같은 프로세스의 생성 직후 경합, 별도 프로세스 set/set·set/delete, 작성자 중단·시간 초과·서비스 중지 후 수동 복구를 검증했다. 아래 R15 ‘미해결’은 수정 전 독립 재현 이력이다. 실제 사용자 서비스·DB·비밀 파일은 사용하지 않았다.

## 2026-09-25 R14 수정 후 전체 독립 확인

R14는 해결 확인했다. 모든 기록된 조상 기준 추적, 발견 후손 영속 보존, 반복 확인·재시작, 생존 시 수동 해제 거부, 빈 조회 시 차단 유지가 코드와 회귀에 반영됐다. 이전 합성 입력은 이제 `[{pid:12,started:'102'}]`를 반환한다. 빌드/typecheck/lint 통과, 전체 Vitest 102개 통과·OS 전용 1개 제외, Edge E2E 1개 통과(14.1초). 이번 환경에서 OS 프로세스 조회가 불가능하여 실제 3세대 OS 전용 시험은 재실행하지 않았다. 아래 R14 미해결 표시는 수정 전 이력이다.

### R15 [P1] 작성 중인 비밀 저장 잠금을 삭제해 동시 저장이 유실될 수 있음 — 미해결

`packages/storage/src/index.ts`의 `AsyncDpapiSecretStore.update`는 `writeFile(..., {flag:'wx'})`로 잠금 파일을 만든다. 파일 생성과 JSON 내용 쓰기는 원자적 단일 동작이 아니다. 경쟁 요청은 그 사이의 빈 파일을 읽어 JSON 파싱에 실패하면 `stale=true`로 판단하고, 재조회 내용도 비어 있으면 살아 있는 작성자의 잠금을 삭제한다. 두 요청이 같은 이전 secrets 데이터를 읽고 각각 덮어쓸 수 있다. 프로세스 간뿐 아니라 같은 프로세스의 별도 store 인스턴스에서도 가능한 경계다.

**독립 재현:** 임시 디렉터리와 합성 값만 사용했다. 첫 잠금의 실제 `fs.open('wx')` 뒤 내용 쓰기를 지연하고 두 번째 저장을 시작했다. DPAPI 변환만 합성 함수로 대체해 두 번째 요청이 기존 내용을 읽은 시점에서 대기시켰다. 첫 저장 완료 후 두 번째 저장을 마무리하면 두 Promise 모두 성공하지만 최종 키는 `fixture-b` 하나뿐이고 `fixture-a`는 사라졌다. 실제 사용자 비밀 파일/DPAPI 값은 사용하지 않았다. 이는 결정적인 파일 I/O 순서 주입 재현이며 실제 별도 프로세스 부하 시험과 구분한다.

**수정 기준:** 비어 있거나 파싱할 수 없는 잠금을 즉시 죽은 소유자로 간주하지 않는다. 작성 중 잠금과 확실히 종료된 소유자의 잠금을 구분하고, 소유권을 입증하지 못하면 제한시간까지 대기 후 실패하도록 한다. 잠금 회수 시 새 소유자의 잠금을 지우는 경합도 함께 검토한다. 비밀 파일의 읽기–변경–교체 전체에 단일 작성자 보장을 유지한다.

**인수 기준:** 생성 직후 빈 잠금/부분 JSON을 관측한 경쟁 요청이 진입하지 않고, 완료 후 두 키가 모두 보존되어야 한다. 같은 프로세스와 별도 프로세스의 set/set·set/delete, 작성자 중단 및 잠금 획득 timeout을 임시 데이터로 검증한다. 내용 없는 잠금의 복구 정책도 문서화한다. 전체 회귀 통과만으로 이 경계를 해결한 것으로 간주하지 않는다.

## 2026-09-25 R14 수정 결과

구현 완료. 모든 저장 조상 기준 추적·새 후손 영속 보존·PID 재사용 세대 구분을 반영했다. 불완전 관측에서 verify 자동 해제를 없애고 명시적 수동 확인만 허용한다. 임시 Windows 실제 3세대 후손 탐지와 모의 OS/임시 DB의 API·재시작 회귀를 통과했다. 실제 사용자 서비스·DB는 미검증이며 아래 두 독립 검토 섹션의 ‘미해결’은 수정 전 발견 기록이다. 전체 검증 결과는 TEST_PLAN.md를 따른다.

## 2026-09-25 RTA-04 후속 독립 검토

### R14 [P1] 종료된 자식 아래의 새 후손을 놓쳐 차단을 해제할 수 있음 — 미해결

`packages/tools/src/process-tree.ts`의 `survivingTree`는 저장된 첫 부모만 가상 루트로 복원한다. 나머지 저장된 프로세스는 PID·생성 시각이 현재 목록과 일치하는지만 검사한다. 저장 시 부모 10(생성 100), 자식 11(생성 101)이 있었고, 자식이 손자 12(생성 102)를 만든 뒤 부모·자식이 종료되면 현재 목록에는 `{pid:12,parentPid:11,started:'102'}`만 남을 수 있다. 이 입력에서 실제 빌드된 함수는 `[]`를 반환했다. PID 재사용이나 OS 조회 실패가 없어도 발생하는 추적 누락이다.

서버의 `/api/v1/safety-blocks/:id/verify`는 이 빈 결과를 전체 종료로 판단해 영속 차단을 해제한다. 종료 직후 검사와 수동 해제 시 생존 검사도 같은 함수를 사용한다. 기존 13개 관련 시험은 통과하지만 이 경계는 포함하지 않는다.

**수정 기준:** 저장한 모든 프로세스 식별자를 후손 추적의 기준으로 사용하고, 재확인에서 새로 관측한 후손도 다음 확인·재시작에 필요한 기록으로 보존한다. PID 재사용 판별을 유지한다. 관측 사이에 중간 조상이 사라져 전체 트리를 확인할 수 없는 경우는 자동 해제 성공으로 간주하지 않는 정책을 정한다. 단순히 첫 부모의 생존 여부만 추가 검사하는 것으로 끝내지 않는다.

**인수 기준:** 부모·자식 종료/새 손자 생존 입력에 대해 잔존을 탐지하고 verify API가 409 및 차단 유지를 반환해야 한다. 손자 확인 후 손자가 새 후손을 만들고 종료하는 반복 확인, 재시작 후 동일 조건, 정상 전체 종료 및 PID 재사용 회귀도 검증한다. 이번에는 합성 프로세스 목록으로 함수의 누락을 재현했으며 해당 3세대 실제 OS 과정과 API 종단 재현은 수행하지 않았다.

빌드와 `rta04-command`, `rta04-safety-api`, `runtime-remediation` 시험 3개 파일·13개가 통과했다. 전체 101개/typecheck/lint/E2E는 이번 독립 검토에서 재실행하지 않았다. 제품 코드와 사용자 서비스·DB는 변경하지 않았다. DB 저장 실패와 OS 조회/종료 경합은 기존 미검증 항목으로 남는다.

## 2026-09-25 RTA-04 후속 독립 검토

### R14 [P1] 종료된 자식 아래의 새 후손을 놓쳐 차단을 해제할 수 있음 — 미해결

`packages/tools/src/process-tree.ts`의 `survivingTree`는 저장된 첫 부모만 가상 루트로 복원한다. 나머지 저장된 프로세스는 PID·생성 시각이 현재 목록과 일치하는지만 검사한다. 저장 시 부모 10(생성 100), 자식 11(생성 101)이 있었고, 자식이 손자 12(생성 102)를 만든 뒤 부모·자식이 종료되면 현재 목록에는 `{pid:12,parentPid:11,started:'102'}`만 남을 수 있다. 이 입력에서 실제 빌드된 함수는 `[]`를 반환했다. PID 재사용이나 OS 조회 실패가 없어도 발생하는 추적 누락이다.

서버의 `/api/v1/safety-blocks/:id/verify`는 이 빈 결과를 전체 종료로 판단해 영속 차단을 해제한다. 종료 직후 검사와 수동 해제 시 생존 검사도 같은 함수를 사용한다. 기존 13개 관련 시험은 통과하지만 이 경계는 포함하지 않는다.

**수정 기준:** 저장한 모든 프로세스 식별자를 후손 추적의 기준으로 사용하고, 재확인에서 새로 관측한 후손도 다음 확인·재시작에 필요한 기록으로 보존한다. PID 재사용 판별을 유지한다. 관측 사이에 중간 조상이 사라져 전체 트리를 확인할 수 없는 경우는 자동 해제 성공으로 간주하지 않는 정책을 정한다. 단순히 첫 부모의 생존 여부만 추가 검사하는 것으로 끝내지 않는다.

**인수 기준:** 부모·자식 종료/새 손자 생존 입력에 대해 잔존을 탐지하고 verify API가 409 및 차단 유지를 반환해야 한다. 손자 확인 후 손자가 새 후손을 만들고 종료하는 반복 확인, 재시작 후 동일 조건, 정상 전체 종료 및 PID 재사용 회귀도 검증한다. 이번에는 합성 프로세스 목록으로 함수의 누락을 재현했으며 해당 3세대 실제 OS 과정과 API 종단 재현은 수행하지 않았다.

빌드와 `rta04-command`, `rta04-safety-api`, `runtime-remediation` 시험 3개 파일·13개가 통과했다. 전체 101개/typecheck/lint/E2E는 이번 독립 검토에서 재실행하지 않았다. 제품 코드와 사용자 서비스·DB는 변경하지 않았다. DB 저장 실패와 OS 조회/종료 경합은 기존 미검증 항목으로 남는다.

## 2026-09-25 최근 개선 전체 재확인

**주요 구현은 반영됐으나 전체 해결·검증 완료는 아니다.** 제품 코드와 사용자 설정은 변경하지 않고 최신 빌드, 소스·명세 대조와 자동 검증을 수행했다.

- UX15: schema v6의 미삭제 행 부분 유일 인덱스, 삭제 이력 보존, 가져오기 충돌 범위, 생성/복제 및 SDK MCP 목록 갱신 회귀를 확인했다. 관련 자동 시험과 삭제 후 동명 재생성 E2E는 통과했다. 실제 사용자 DB 마이그레이션을 이번 검토에서 수행/확인하지 않았다.
- UX16: 모델 기본 티어와 에이전트 상속/재정의/필드 생략, 요청·실제 티어 구분, 고급 값 우선순위가 연결됐다. 모의 시험과 UI E2E 통과. 지원 판정은 OpenAI 프로필+openai-chat+직접 API 주소에 한정되므로 다른 공급자까지 서비스 티어 구현 완료로 볼 수 없다. 실제 계정/모델 적용·청구/속도도 미검증이다.
- T01: 변경 대상 ID의 성공 이벤트→관리 API→시험·기록 화면 연결은 유지된다. 새 대상 생성·결과 표시 E2E 통과. 정책별 대상 경계 전체와 파일 경합, 실제 라이트 대상 지정 실행 검증은 여전히 남는다. 기존 직접 경로 실모델 시험은 대상 ID 계약의 실환경 검증을 대신하지 않는다.

### R13 [P2] 대상 통합 시험의 기본 timeout 부족 — 미해결

`tests/target-binding.test.ts:72`의 여러 서버/UI API/MCP 시나리오가 하나의 기본 5000ms 제한에 묶여 있다. 이번 전체 단일 worker 실행에서 79개 통과·1개 timeout 실패였고, 해당 파일만 `--testTimeout=15000`으로 재실행하니 2개 통과(통합 시험 6750ms)했다. 이전에도 반복된 검증 안정성 문제이며 기본 명령 전체 통과로 보고하면 안 된다. 제품 동작 assertion 실패는 재현되지 않았다. 해당 통합 시험에 현실적인 개별 상한을 지정하거나 독립 시나리오로 분리하고 기본 전체 명령으로 재검증하는 것이 수정 기준이다. 이번 검토는 코드/테스트 설정을 변경하지 않았다.

빌드/typecheck/lint 통과, 전체 Vitest 25개 파일 중 24개 통과·1개 실패, 80개 중 79개 통과·1개 실패. 실패 파일은 위 상한 조정 실행에서 통과했으며 Edge E2E 1개는 17.4초로 통과했다. 콘솔 깜빡임 실제 화면 관찰·외부 클라이언트 고유 제한시간은 이번에도 확인하지 않았다.

## 2026-09-24 T01 후속 독립 확인

변경 대상 ID의 성공 이벤트→관리 API targetChanges→시험/실행 기록 화면 연결을 확인했다. 실패한 작업·읽기 작업을 변경으로 집계하지 않고, 공급자 실패 이전 변경과 보존 만료 구분을 유지한다. 최신 build/typecheck/lint와 대상 화면 Edge E2E가 통과했고 대상 테스트 2개도 단독 재실행에서 통과했다. 이전 T01 연결 누락은 해결됐다.

검증 주의: 대상 통합 시험은 다른 검사와 동시에 실행할 때 5초 timeout 1회, 단독 실행은 4.456초로 통과했다. 테스트 안정성 개선 여지는 남으며 상세는 TEST_PLAN을 따른다. 이번 T01 범위에서 추가 제품 결함은 확인하지 못했다. 실제 라이트·정책 조합·경합 검증은 여전히 별도다.

## 2026-09-24 대상 지정 1차 구현 확인

최초 재검토에서는 코드·설계·시험을 대조하고 전체 빌드와 `tests/target-binding.test.ts`를 재실행했다(2개 통과). 그 재검토 자체는 제품 코드·사용자 설정을 수정하지 않았다. 이후 아래 T01 수정과 자동 검증을 수행했으며 전체 인수 완료로 판단하지 않는다.

### T01 [P2] 변경 대상 결과의 관리 API→화면 연결 — 수정·자동 검증 완료

2026-09-24 수정: 성공한 `tool.finished`의 `write_target`/`replace_target`만 실행별 `targetChanges`로 복원해 GET `/runs`, GET `/runs/:id` 및 취소 응답에 연결했다. 시험 결과와 실행 기록에 ID·작업을 표시한다. 실패·취소 전 발생한 변경도 유지하고, 보존 만료된 실행은 `null`로 내려 확인 불가를 표시한다. 아래 문제 설명은 수정 전 이력이다.

모의 실행에서 읽기 전용은 빈 변경, 생성·치환은 성공 ID, 생성 뒤 공급자 실패는 부분 변경, 새로고침 뒤 동일 결과, 보존 만료는 확인 불가를 검증했다. 새 대상 UI의 Edge E2E에서 생성 결과와 실행 기록 재조회 표시를 확인했다. 실제 라이트 모델과 더 넓은 경계·경합 검증은 별도로 남는다.

최종 build/typecheck/lint, Vitest 24개 파일·78개, Edge E2E 1개 통과. E2E의 첫 실행은 대상 입력 선택자가 다른 그리드를 가리켜 대기 만료됐고 선택자 수정 후 반복 2회 통과했다. 사용자 저장 설정이나 실제 모델은 변경·호출하지 않았다.

- 서버는 성공한 write_target/replace_target 결과를 observations.changes의 targetId로 수집하고 MCP 응답에 포함한다. tool.finished 성공 이벤트에도 targetId/name/ok를 저장한다.
- 그러나 실행 조회의 runPublic은 observations.changes를 반환하지 않고 finishRun에도 변경 목록을 별도 저장하지 않는다. 시험 UI는 최종 답변·검증 근거·오류·원시 실행 상세를 표시할 뿐, 실제 변경된 targetId 요약이 없다. 따라서 단순 UI 라벨 추가만으로 해결되지 않는다.
- 해결 기준: 성공한 도구 이벤트에서 변경 목록을 구성해 관리 API에 제공하거나, 실행 관측값을 저장·조회하도록 연결한 뒤 시험 결과와 실행 기록에 targetId/작업을 명시한다. 원본 input.targets나 스냅샷의 대상 목록을 실제 변경 목록으로 표시해서는 안 된다.
- 파일 쓰기 성공 뒤 모델 요청이 실패/취소되는 경우에도 이미 발생한 변경을 표시한다. 읽기만 수행하거나 도구가 실패한 경우 변경으로 집계하지 않는다. 보존 만료로 근거가 없으면 '변경 없음'으로 오인시키지 않는다.
- 인수 기준: 생성/치환 성공의 ID 표시, 읽기 전용·실패 제외, 부분 변경 뒤 실패 결과, 새로고침 후 같은 표시를 모의 실행과 새 대상 UI E2E로 확인한다.

### 확인 범위와 남은 검증

대상 ID/권한/해시/배타 생성 단위 시험과 fixed 정책의 모의 UI API·MCP 읽기 통합 시험은 통과했다. 현재 대상 통합 시험의 모의 모델은 read_target만 호출하므로 서버 경유 입력 읽기→출력 생성 전체 흐름과 변경 ID의 결과 연결까지 검증한 것은 아니다. 새 대상 편집 UI E2E, caller/full/none 조합 및 대기 후 경로 교체·동시 생성 경합, 실제 라이트 모델 입출력은 남아 있다. 기존 경로 도구 시험과 기존 E2E 통과를 새 대상 실행 전체의 통과로 확대 해석하지 않는다.

## 2026-09-23 개선 후 독립 재검토

제품 코드를 변경하지 않고 현재 소스와 최신 빌드로 재검증했다. A01 scope 전달, 기존 에이전트 보존/수정 안내, A02·A04·A05의 제한시간 메타데이터·분리 정책·UI, A06 진단 기록, A07 실행/과제 검증 구분 및 UX10 모델 표시·UX11 PowerShell 숨김 옵션을 확인했다. build/typecheck/lint, Vitest 23개 파일·75개 테스트, Edge E2E 1개(9.4초, exit 0)가 통과했다.

### B01 [P2] 전체 접근에서 실행할 수 없는 경로 복구 안내 — 수정·검증 완료

후속 수정: ENOENT/ENOTDIR와 PATH_ERROR의 안내를 실행 정책·허용 도구에 맞게 구분했다. scoped는 목록 도구가 허용된 경우 `.` 탐색, full은 사용자가 제공한 경로의 존재하는 상위 폴더를 절대 경로로 탐색하도록 안내한다. 목록 도구가 없으면 경로 재확인만 안내한다. 실제 루트 문자열을 넣지 않는다. 아래 재현은 수정 전 이력이다.

검증: scoped/full·목록 허용/미허용 조합의 ENOENT/ENOTDIR 및 PATH_ERROR 회귀를 포함해 관련 20개 테스트, 도구 패키지 빌드, 전체 typecheck/lint 통과. 허용된 목록 도구를 안내된 경로 형식으로 실행해 성공했다.

- 위치: `packages/tools/src/index.ts`, executeWorkspaceTool의 ENOENT/ENOTDIR 및 PATH_ERROR 처리.
- 파일을 찾지 못하면 정책과 무관하게 `list_files(".")`로 실제 상대 경로를 확인하라고 안내한다. 그러나 full 모드는 기준 폴더가 없으며 절대 경로만 허용한다.
- 최신 빌드의 `new WorkspaceTools(null)`에서 존재하지 않는 절대 경로에 read_file을 호출해 ENOENT 안내를 확인했다. 이어 안내대로 list_files에 path=`.`를 전달하자 PATH_FORBIDDEN(전체 접근에서는 절대 경로 필요)으로 실패했다. 파일 생성/수정·클라우드 호출 없이 재현했다.
- 영향: 전체 접근 에이전트가 오류 메시지를 따라 재시도해도 다시 실패하여 모델 요청과 도구 예산을 낭비한다. 범위 내부 절대 경로 지원 자체의 실패는 아니다.
- 수정 기준: scoped는 `.` 탐색, full은 사용자가 제공한 절대 경로의 상위 폴더 등 **절대 경로로 탐색**하도록 구분한다. 임의의 드라이브/기준 폴더를 선택하거나 실제 설정 루트를 메시지에 주입하지 않는다. list_files가 미허용이면 이를 항상 실행 가능한 복구 수단처럼 안내하지 않는지도 함께 검토한다.
- 회귀 기준: scoped/full 각각 없는 파일 및 ENOTDIR 실패 뒤 제시한 경로 형식으로 허용된 목록 도구를 호출할 수 있어야 한다. 정상 상대/절대 경로, 범위 제한, 해시 보호 회귀를 유지한다.

### 검증 한계

- 실제 Windows 화면에서 콘솔 깜빡임이 사라졌는지는 확인하지 않았다. DPAPI 및 보조 프로세스 숨김 옵션의 코드 반영과 기존 자동 검증만 확인했다.
- 사용자 DB와 저장 에이전트를 수정/조회하지 않았으므로 기존 공개 에이전트의 scope 누락이 실제로 교정됐다고 선언하지 않는다. 변경되지 않은 기존 설정은 사용자의 초안 반영·적용이 필요하다.
- 외부 클라이언트 고유 제한시간, 실제 클라우드 모델 입출력, UNC·실제 다중 드라이브는 이번 검증에 포함하지 않았다. 현재 분리 시간 정책은 명시적으로 선택하는 방식이며 기존 전체 제한 설정은 유지된다.

## 2026-09-23 UX10–UX12 후속 구현

모델 표기를 프로바이더명으로 식별하도록 수정하고, DPAPI PowerShell을 숨김 실행하며, 파일·명령 경로의 모호한 입력과 파일 없음 복구 안내를 공통 도구 경계에서 보완했다. 기존 저장 참조와 fixed/caller 범위·해시 보호는 유지한다. 실제 사용자 데스크톱의 깜빡임 원인과 외부 클라우드 모델·UNC·다중 드라이브 시험은 자동 검증과 구분해 미검증으로 남긴다.

build/typecheck/lint, Vitest 단일 worker 23개 파일·75개 테스트, Edge E2E 1개 통과. 마지막 ENOTDIR 재탐색 보완 후 P4 18개도 다시 통과했다.

## 2026-09-23 A07 실행 결과 의미 구현

기존 `completed`·MCP `outcome=succeeded`는 실행 정상 종료로 유지하면서 `verification`을 별도 추가했다. 현재는 명시적 과제 합격 기준이 없어 `not_verified`만 자동 기록하고, 명령 종료 코드·도구 실패 코드를 실제 근거로 제공한다. 화면에 **실행 완료 · 과제 미검증**을 명시한다. 도구 실패 뒤 답변한 실행과 정상 무변경 응답을 과제 성공/실패로 단정하지 않는다. 공급자 요청 시간, 모델 반복, 도구 횟수 제한의 오류 코드와 terminal 상태를 구분했다.

build/typecheck/lint, Vitest 단일 worker 22개 파일·73개 테스트, Edge E2E 1개 통과. 외부 모델의 과제 충족 여부와 외부 공급자의 실제 시간 초과는 확인하지 않았다.

## 2026-09-23 A06 도구 실패 진단 구현

실패 `tool.finished` 이벤트에 안전한 `diagnostic`을 추가했다. 경로의 절대·상대·모호·누락 표기, 허용 루트 기준 상대 대상, expectedHash 제공 여부, 정형 이유를 기록한다. 범위 밖 절대 경로·full 접근 경로는 대상 문자열을 마스킹하고 원시 인수·파일 내용·해시·예외 메시지는 기록하지 않는다. 모의 공급자의 범위 밖 경로/없는 파일/해시 누락/해시 충돌 회귀를 통과했다. build/typecheck/lint, Vitest 단일 worker 72개, Edge E2E 1개 통과. 다음 구현 단위는 A07 결과 의미 개선이다.

## 2026-09-23 A02·A04·A05 시간 정책 구현

기존 `timeoutMs`의 접수 기준 전체 제한을 유지하고 분리 정책에는 `queueTimeoutMs`·`executionTimeoutMs` 두 필드를 도입했다. 새 분리 정책은 각 1초~60분, 대기 초기값 120초, 브리지 응답 여유 15초로 확정해 API/DATA_MODEL에 기록했다. STDIO 브리지는 적용된 도구의 공개 메타데이터에서 서버 최대 대기를 읽어 backend timeout으로 사용한다. 큐 상태·오류를 대기/실행/취소로 분리하고 UI 시간 설정·실행 기록을 연결했다. 기존 에이전트와 적용 버전은 자동 전환하지 않는다.

가상 61초 브리지 호출, 큐 단계별 만료·실행 예산, DB v5 마이그레이션을 자동 검증했다. build/typecheck/lint, Vitest 단일 worker 72개와 Edge E2E 1개가 통과했다. 외부 클라이언트 자체 timeout, 실제 사용자 DB 및 외부 공급자의 장시간 호출은 미검증이다. 다음 구현 단위는 A06 진단이다.

## 2026-09-23 A01 수정 및 검증

기본 `코드 구현` 템플릿의 사용자 메시지에 `{{input.scope}}`를 추가했다. 코드 조사·구현 템플릿의 `workspace`는 선택 문맥 입력으로 설명하고 실제 파일 도구 기준을 바꾸지 않는다는 일반 안내를 시스템 메시지에 넣었다. 서버 시작 시 코드가 소유한 기존 기본 템플릿 정의를 갱신하되 저장 에이전트의 초안과 적용 버전은 그대로 둔다. 에이전트 목록은 적용 버전의 scope 누락 여부를 알리고 화면은 초안 후보 목록, 템플릿 차이 보기와 메시지만 적용하는 선택지를 제공한다.

로컬 모의 공급자 요청에 고유 scope 문자열이 들어가는지, 기존 에이전트·적용 버전이 재시작 후 보존되는지, 사용자가 템플릿의 메시지만 초안에 반영한 뒤 다시 적용할 때까지 기존 공개 버전이 유지되는지 확인했다. 당시 build/typecheck/lint, Vitest 22개 파일·70개 테스트, Edge E2E 1개가 통과했다. 실제 사용자 DB의 영향 에이전트 개수와 외부 모델에서의 동작은 해당 검증에 포함하지 않았다.

## 2026-09-23 후속 해결 방안 — 구현 인계 계획

**A01, A02·A04·A05, A06, A07은 위 절에서 수정·자동 검증 완료했다.** 최초 인계 단계에서는 소스와 기존 기록을 대조했으며 로컬 DB를 다시 조회하거나 외부 모델을 호출하지 않았다. A01–A07의 과거 DB 분석 근거는 아래 실사용 시험 재검토 절을 따른다.

### 우선순위와 구현 단위

| 순서     | 대상                                   | 해결 방안                                                                                                                                                             | 인수 기준                                                                                                     |
| -------- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| 1 · 완료 | A01 scope 누락                         | 코드 구현 템플릿의 scope 전달과 기본 템플릿 갱신, 기존 에이전트 영향 후보·차이 보기·선택 적용을 구현했다. 저장 에이전트의 초안·공개 적용 버전은 자동 교체하지 않는다. | 고유 scope 문자열이 모의 공급자 요청에 도달하고 기존 초안·적용 버전 보존 및 명시적 재적용을 검증했다.         |
| 2 · 완료 | A02·A04·A05 시간 정책                  | 기존 전체 제한은 유지하고 대기/실행 분리 정책, 브리지 동적 timeout, 초 단위 UI, 실행 구간 표시를 구현했다.                                                            | 가상 61초 모의 응답·대기 후 실행 예산·단계별 만료·취소 자동 검증. 외부 클라이언트 timeout은 미검증이다.       |
| 3 · 완료 | A06 진단                               | 실패 이벤트에 경로 표기, 허용 루트 내 상대 대상, 해시 제공 여부, 정형 이유를 추가했다. 범위 밖·full 절대 경로와 원시 인수는 저장하지 않는다.                          | 모의 범위 밖 경로·없는 파일·해시 누락·해시 충돌 구분과 절대 루트·파일 본문 비노출을 검증했다.                 |
| 4 · 완료 | A07 결과 의미                          | 기존 실행 상태·MCP outcome은 유지하고 별도 미검증 상태와 실제 명령·도구 근거를 추가했다. 시간·모델 반복·도구 횟수 제한 코드를 구분했다.                               | 도구 실패 뒤 답변·무변경 응답 모두 과제 판정을 자동 부여하지 않고, 기존 MCP 필드와 새 필드의 공존을 확인했다. |
| 병행     | 모델 중복 표시·창 깜빡임·A03 경로 의미 | UX10–UX12의 구체적 작업과 회귀 기준을 따른다.                                                                                                                         | [실사용 개선 문서](USABILITY_ISSUES.md)의 해당 절 참조.                                                       |

### 시간 정책 결정과 남은 외부 확인

- 기존 `timeoutMs`는 접수부터의 전체 제한으로 유지했다. 새 대기/실행 필드 두 개를 함께 설정해야 분리 정책을 적용한다. 범위·초기값·15초 브리지 응답 여유는 API_SPEC/DATA_MODEL을 따른다.
- SDK 내부 timeout과 외부 메인 클라이언트 timeout은 별개다. 서버가 유효 시간을 MCP 도구 메타데이터에 게시해 브리지에 전달한다. 외부 클라이언트 한도를 넘는 작업의 비동기 접수/결과 조회는 필요성이 확인되면 별도 범위로 설계한다.
- 큐 대기·실행·전체 시간을 분리하고 현재 대기 원인(동시 실행 한도/리소스 그룹/작업 폴더 잠금)을 표시한다. 같은 폴더 잠금은 제거하지 않았다.

### 추가 소스 확인

- 모델 선택기는 `apps/web/src/main.tsx`의 ModelOptions가 `label (modelId)`를 사용하고 등록 시 label=modelId를 저장한다. 중복 표시 원인이 코드에서 확인됐다.
- `packages/storage/src/index.ts`의 DpapiSecretStore.transform은 PowerShell spawnSync에 windowsHide가 없다. set/get마다 해당 경로가 실행되므로 창 깜빡임의 유력 후보다. 반면 VBS 진입점은 창 스타일 0, 서비스 시작·브라우저 열기·파일 명령/종료·프로세스 확인 경로는 숨김 옵션이 있다. 실제 깜빡이는 창의 PID/부모 프로세스는 확인하지 않았으므로 발생 원인 확정이나 수정 완료로 기록하지 않는다.
- safePath는 이미 상대/범위 내부 절대 경로를 공통 처리한다. 남은 문제는 문맥상의 프로젝트 경로와 실제 기준 폴더 혼동, 모호한 Windows 경로 입력 및 복구 안내다. UX09를 미구현 문제로 다시 분류하지 않는다.

## 2026-09-23 실사용 시험 재검토 — 공급자 장애 제외

현재 소스, 설치된 MCP SDK, TEST_PLAN의 시험 이력과 로컬 DB의 실행 이벤트를 대조했다. 외부 모델을 재호출하지 않았고 제품 코드·설정은 변경하지 않았다. 아래 항목은 조사 확인이며 수정 완료를 뜻하지 않는다. 과거의 해결 완료 항목과 구분한다.

| 항목                                   | 분류·우선순위              | 확인된 문제와 영향                                                                                                                                                                                                    | 근거·개선 방향                                                                                                                                                                                                                                                                      |
| -------------------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A01 입력 필드 누락                     | 결함 / P1                  | 기본 coding 입력 스키마에는 scope가 있지만 userPromptTemplate에는 없어, 해당 필드에 넣은 수정 범위 지시가 모델 메시지에서 사라진다. 세 코딩 에이전트 시험에도 같은 누락이 있었다.                                     | apps/server/src/index.ts의 builtinTemplateDefinitions 및 initialMessages. scope 렌더링과 입력→실제 메시지 회귀 검증 필요. 템플릿 수정만으로 기존 저장 에이전트까지 고쳐지는지는 별도 확인해야 한다.                                                                                 |
| A02 제한시간 불일치                    | 결함 / P1                  | STDIO 브리지는 backend.callTool에 signal만 넘겨 설치 SDK 기본 60초 제한을 적용한다. 서버 기본 실행 제한은 120초다. 서버 완료 전에 브리지 응답이 끊길 수 있다.                                                         | apps/cli/src/tool-bridge.ts:67, SDK DEFAULT_REQUEST_TIMEOUT_MSEC=60000, 서버 submitRun timeoutMs. 과거 클라이언트 timeout 뒤 서버 완료 이력과 부합하지만 개별 Codex timeout의 발생 계층 전부를 확정한 것은 아니다. 브리지·서버·외부 호출자 대기 정책 정합성 필요.                   |
| A03 작업 폴더 의미 혼동                | 설정·UX 제약 / P2          | input.workspace는 프롬프트의 문자열이고 fixed 모드의 실제 도구 기준 폴더를 변경하지 않는다. 고정 루트가 C:\Users인 시험에서 프로젝트 기준 상대 경로라는 가정은 성립하지 않았다.                                       | resolveRunWorkspace, initialMessages, WorkspaceTools 경로 계약. 경로 오류의 정확한 인수는 로그에 없어 개별 실패 원인 단정 불가. 허용 범위와 작업 기준 폴더의 차이를 명확히 안내해야 한다. 실제 루트 문자열을 도구 설명에 자동 공개하는 방식은 사용자의 경로 비공개 요구와 충돌한다. |
| A04 넓은 폴더 잠금과 대기 중 시간 소진 | 실행 정책·관측성 제약 / P2 | 같은/겹치는 workspace는 실행 전체 동안 직렬화되고 deadline은 접수 시 시작한다. 서로 다른 출력 파일이어도 같은 fixed 루트면 대기 시간이 실행 예산을 소진한다.                                                          | packages/runtime/src/index.ts의 WorkspaceLockManager와 RunQueue, 서버 deadlineAt. 대기 시간·실행 시간 분리 표시와 제한시간 의미 명시 필요. 잠금 제거가 해결책이라는 뜻은 아니다.                                                                                                    |
| A05 실행 제한 설정 UI 누락             | UI 기능 공백 / P2          | 서버가 runtime.timeoutMs를 지원하지만 웹 AgentConfig와 편집 화면에 해당 설정이 없다. 사용자가 화면에서 긴 코딩 작업의 제한시간을 조절할 수 없다.                                                                      | apps/web/src/main.tsx 및 서버 runtime 타입/submitRun. 서버 필드와 편집 UI를 연결하고 브리지 제한도 함께 고려해야 한다.                                                                                                                                                              |
| A06 진단 로그 부족                     | 관측성 결함 / P2           | tool.started는 이름·callId, 실패 tool.finished는 오류 코드 등을 기록하나 입력 경로·오류 메시지·해시 사용 여부를 남기지 않는다. PATH_FORBIDDEN/ENOENT/HASH_CONFLICT의 정확한 호출 원인을 사후 복원할 수 없다.          | 서버 tool.started/tool.finished 기록. 비밀·파일 본문을 저장하지 않는 마스킹된 진단 정보가 필요하다.                                                                                                                                                                                 |
| A07 실행 종료와 과제 성공 혼동         | 결과 계약·평가 제약 / P2   | 모델이 도구 없이 최종 답변을 내면 루프가 끝나며, 출력 형식 검증 후 completed/succeeded가 된다. 파일 수정 실패 뒤 계획만 답해도 이 상태가 가능하다. 시간·턴 수·도구 수 한도도 DEADLINE 계열로 묶여 원인 구분이 약하다. | runToolLoop, executeRun, MCP 결과 매핑. code_light 자율 시험은 6회 도구 실패·변경 0건인데 completed였다. 과제 인수 검증 결과와 실행 상태를 분리하고 종료 사유를 구분해야 한다. 일반 채팅까지 파일 변경을 성공 조건으로 강제해서는 안 된다.                                          |

### 동시 시험의 재분류 근거

test3 배치의 첫 실행 e4f35923-b639-4c54-86b3-eef50679f907은 UTC 2026-09-22 18:05:21.709에 시작해 18:07:21.724에 종료됐다. 뒤의 e7d9911a-2ef1-4b17-85a9-bf506af7d075, a412abae-0e99-4527-8a95-479701815818, 7d0375da-113b-4f45-af01-fa9cc5955902는 각각 약 119초 대기 후 시작해 약 1초 만에 timed_out됐다. 뒤 세 건도 model.started 이벤트는 있으므로 '모델 호출 0회'라고 해석해서는 안 된다. 동시 접수와 동시 모델 실행은 다르며, 이 배치는 모델 자체의 동시 4건 처리 능력을 평가한 결과로 사용할 수 없다.

durationMs는 Date.now() - run.created_at이며 큐 대기·모델·도구 시간을 포함한다. 과거 응답에서 이를 providerMs 또는 순수 추론 시간으로 부른 것은 잘못이다. 왕복 지연과 서버 전체 처리시간, 모델 적재·추론 속도는 구분해야 한다.

### MCPex 결함으로 단정하지 않는 항목 및 기존 평가 정정

- expectedHash 지침이 없다는 기존 설명은 잘못이다. write_file/replace_text 도구 설명에 계약이 있고 EXPECTED_HASH_REQUIRED 메시지도 read_file을 안내한다. HASH_CONFLICT는 보호 동작이며 실제 동시 수정이나 저장 손상의 증거가 아니다. 모델의 인수 오류·복구 실패와 구분한다.
- scoped 절대 경로 거부는 UX09 이전 제약이며 9월 21일 범위 내부 절대 경로 지원으로 수정·검증됐다. 현재 미해결 문제로 다시 세지 않는다. 범위 밖 접근 거부와 caller 메타데이터 검증 자체도 정상 경계 정책이다.
- run_command 미제공은 해당 에이전트의 도구/명령 allowlist 설정에 따른다. 비활성 에이전트의 MCP 목록 미노출도 정상이며, 세 코딩 에이전트 평가는 공개 MCP 호출이 아닌 초안 시험이었다.
- 공급자 429/500, 연결 단절, 원인 미확정 400은 이번 결함 목록에서 제외한다. advance의 400만으로 tool-call 비호환을 확정할 수 없다. middle은 도구 실행 후 후속 모델 요청에서 429가 발생했으며 외부 계정 사용이나 거절 요청 과금이 원인이라는 기존 추정은 입증되지 않았다. 전체 대화 재전송에 따른 입력 누적은 확인되지만 공급자 장애와 구분해야 한다.
- 20회 정확 문자열 시험은 파일 입출력 검증이 아니다. scope 누락 상태에서 무변경을 근거로 범위 지시 준수를 검증했다고 볼 수 없고, 변경 없는 복사본의 테스트 점수로 모델 코딩 실력을 순위화할 수 없다.
- 시험 스크립트의 PowerShell 문법, SSE를 JSON으로 파싱한 오류, 평가 환경 DPAPI 접근 실패는 MCPex 제품 결함으로 합산하지 않는다.

수정 우선순위는 A01·A02, 이후 A06·A05와 상태/시간 표시 개선이다. 이번 작업은 원인 조사와 문서 정정이며 새 코드 수정·회귀 실행은 하지 않았다.

## 2026-09-21 UX09 구현 확인

scoped 작업 폴더의 파일 경로와 `run_command.cwd`가 상대 경로 및 범위 내부 절대 경로를 같은 대상으로 처리하도록 개선했다. 공개 MCP 설명과 내부 모델 도구 설명에는 실제 작업 폴더 문자열을 넣지 않고 일반 범위 계약만 표시하며, 파일 도구 결과도 가능한 경우 상대 경로를 반환한다. 범위 밖·부모 이동·유사 접두사·junction 우회 차단과 `fixed`·`caller`·`full` 정책 구분은 유지된다.

build, typecheck, lint, Vitest 22개 파일·69개 테스트와 Edge E2E 1개가 통과했다. Edge E2E는 약 11.3초 후 수동 개입 없이 exit 0으로 종료됐다. 실제 NVIDIA 공급자 HTTP 500은 별개 문제이며, UNC·실제 다중 드라이브와 실제 클라우드 모델 입출력은 이번 자동 검증에 포함하지 않았다.

## 2026-09-18 독립 재확인 및 실제 MCP 호출

N04·N05 수정을 재확인했다. 빌드·typecheck·lint와 21개 파일·68개 테스트, Edge E2E 1개가 통과했다. E2E는 약 8.2초 후 수동 개입 없이 종료됐다. 별도 빌드 산출물 검사에서 junction root는 PATH_FORBIDDEN으로 거부되고 명령 ID enum·label 전달, executable 비노출, 빈 allowlist의 run_command 제외를 확인했다. 이번 검토 범위에서 새로운 차단 문제는 재현하지 못했다.

사용자 승인으로 현재 Codex에 등록된 MCPex의 5개 도구를 실제 호출했다. chat은 지정된 확인 문자열, doc_tool은 제공 문서 요약, architecture는 제공 설계에 대한 확인 항목을 반환했다. code_research는 package.json의 name=mcpex와 scripts.test=vitest run, coding은 engines.node=>=24를 반환했으며 로컬 파일과 대조했다. 두 파일 도구 실행은 저장된 이벤트에서도 각각 read_file 1회·ok=true였고 모든 결과의 changes/checks는 빈 배열이었다. 5개 모두 completed/succeeded였다.

실제 연결·목록 노출·응답 및 읽기 전용 도구 호출은 확인했다. 재연결·동적 활성 목록 변경·취소·실제 쓰기/명령 실행·다른 모델/외부 공급업체·백업 복구까지 완료한 것은 아니다. 제품 코드·사용자 설정은 변경하지 않았으며 실제 호출에 따른 실행 기록만 추가됐다.

## 2026-09-18 수정 후 전체 재검토 — N04·N05 해결

**N01–N05와 E2E 종료 지연을 모두 해결했다.** 아래 이전 검토 절은 이력이며 현재 판단은 이 절을 따른다.

### N04 [P1] 호출 작업 폴더 자체가 junction이면 허용 루트 경계를 벗어남 — 수정·검증 완료

- 위치: `apps/server/src/index.ts:414`의 `resolveRunWorkspace`, `packages/tools/src/index.ts:82–95`의 `safePath`.
- 호출 폴더는 문자열 경로의 포함 관계만 확인한다. 파일 도구의 링크 검사는 작업 폴더 아래의 경로 구성요소부터 시작하므로 작업 폴더 자체와 그 상위 경로의 junction을 검사하지 않는다.
- Windows 임시 디렉터리 안에 `allowed`와 `outside`를 만들고 `allowed/project`를 `outside`를 가리키는 junction으로 구성했다. caller 허용 루트를 `allowed`, 요청 workspace를 `allowed/project`로 설정해 모의 모델이 `read_file('fixture.txt')`를 호출했다. 실행은 completed였으며 `outside/fixture.txt`의 시험 문자열이 모의 모델의 다음 요청에 전달됐다. 실제 API→작업 폴더 해석→도구→모델 흐름으로 확인했다. 개인 파일이나 외부 모델은 사용하지 않았으며 시험 junction·파일·DB를 모두 정리했다.
- 영향: caller에서 제한한 루트 밖 파일이 읽히고 쓰기 도구도 같은 경로 해석을 사용한다. scoped 폴더의 논리 경로와 실제 경로가 달라 잠금 중첩 판정도 영향을 받을 수 있다. 이는 full 모드에서 사용자가 허용한 넓은 접근과 별개의 문제다.
- 수정 기준: 선택 작업 폴더와 상위 경로까지 링크 정책을 적용하고 실제 경로 기준 허용 루트 포함 관계를 확인한다. 실행 도구와 큐가 같은 검증된 경로를 사용하게 한다. 루트 자체·중간 상위 경로 junction, 허용/비허용 대상, 일반 폴더 회귀를 추가한다. 현재의 링크 전면 차단 정책과 일치시켜야 한다.
- 2026-09-18 수정: 파일시스템 루트부터 선택 폴더까지 각 구성요소를 `lstat`으로 검사해 심볼릭 링크와 Windows junction을 거부하고, `realpath`로 확정한 실제 경로를 허용 루트 포함 검사·실행 스냅샷·큐 잠금·도구 root에 공통 사용한다. 도구 호출 때 root를 다시 검사해 접수 후 링크 교체도 거부한다.
- 검증: 정상 root의 실제 경로 확정, root 자체와 중간 상위 junction 거부, caller API의 junction workspace HTTP 403을 회귀 검사했다. 기존 정상 caller UI/MCP 흐름도 유지됐다.

### N05 [P2] 모델에게 사용 가능한 commandId 목록을 전달하지 않음 — 수정·검증 완료

- 위치: `apps/server/src/index.ts:2366–2388`, `packages/tools/src/index.ts:478–488`.
- 서버는 `commands`를 실제 실행기의 allowlist로만 넘긴다. 모델의 `run_command` 스키마에는 임의 문자열 commandId만 있고 등록한 ID·label은 시스템 메시지나 도구 정의에 포함되지 않는다.
- 임시 에이전트에 고유 commandId `private_test_runner_942`, label `Run test suite`를 등록하고 `run_command`만 허용했다. 실제 서버가 로컬 모의 모델로 전송한 전체 요청을 확인한 결과 두 값 모두 없었다. 사용자 메시지는 일반적인 실행 요청이었으며 특정 ID를 미리 알려주지 않았다. 이 검사는 외부 명령을 실행하지 않고 전달 계약만 확인했다.
- 영향: 사용자 프롬프트에 명령 ID를 다시 적지 않으면 모델이 올바른 명령을 선택할 근거가 없다. 기존 명령 테스트는 모의 모델이 `node-check`를 하드코딩해 이 누락을 탐지하지 못한다. 추측한 ID가 다르면 COMMAND_NOT_ALLOWED로 실행이 실패한다.
- 수정 기준: 적용된 명령의 ID와 label을 도구 설명 또는 commandId enum 등으로 모델에 전달한다. 실행 파일 경로·비밀값을 불필요하게 노출할 필요는 없다. allowlist가 비어 있으면 실효 도구 상태와 모델 제공 도구에서 사용 불가능함을 반영한다. 임의의 사용자 정의 ID를 모델이 전달받는 회귀 검증을 추가한다.
- 2026-09-18 수정: 실행 시 생성하는 `run_command` 정의의 `commandId`를 적용된 allowlist의 enum으로 제한하고 ID와 label을 설명에 추가한다. executable 경로는 공개하지 않는다. 명령이 없으면 `run_command`를 모델 도구와 연결 정보의 실효 도구에서 제거하고 MCP read-only annotation도 같은 실효 목록을 사용한다.
- 검증: 고유 ID `private_test_runner_942`와 label이 실제 로컬 모의 모델 요청의 설명·enum에 포함되고 executable은 없는지 확인한 뒤 해당 ID로 명령 실행까지 통과했다. 빈 allowlist에서는 `run_command` 정의와 연결 정보의 실효 도구가 제거되는 회귀도 통과했다.

### 이번 재검증 결과

- build/typecheck/lint 및 `git diff --check` 통과.
- Vitest: N04 회귀 추가 후 21개 파일·68개 테스트 통과.
- Edge E2E: 1개 시나리오 통과, 전체 약 8초, 수동 종료 없이 exit 0.
- N01: 빌드 산출물의 실제 동시 ensureService 두 호출 모두 fulfilled, health 200. 임시 데몬·데이터 정리 완료.
- N02: 정책별 도구 required·절대 경로 설명, 실제 모의 모델 요청과 실행을 포함한 회귀 통과.
- N03: 부모 잠금 중 일반 자식·`..cache`는 모두 차단되고 독립 폴더는 허용됨을 재확인.
- N04: root 자체·중간 상위 junction과 caller API 경계 차단, 정상 폴더의 실제 경로 스냅샷·실행을 확인.
- N05: commandId enum·label 모델 전달, executable 비공개, 빈 allowlist의 `run_command` 비활성화를 확인.
- Edge E2E는 첫 실행에서 기존 초안 복원 단계의 UI 값 반영 대기로 실패했으나 동일 빌드 즉시 재실행은 1 passed, 약 8초, exit 0이었다. N05 관련 build/typecheck/lint와 서버 통합 회귀는 첫 실행부터 통과했다.
- 실제 사용자 MCP 클라이언트 영구 등록·실모델 호출·전체 백업 복구·다중 드라이브/UNC·실제 ACL 거부 검증은 미수행이다. 이번 junction 검사는 통제된 로컬 재현이며 모든 파일시스템 경계를 검증한 것은 아니다.

## 2026-09-18 전체 재검토 — 최신 판단

**기본 자동 검증은 통과했지만 전체 인수 완료는 아니다.** 현재 작업 트리의 서버·웹·CLI·저장소·도구·큐 연결과 UX06–UX08 변경을 대조했다. N01–N03은 후속 수정·검증했다. 이 절의 판단이 이전 절의 “남은 연결 문제 없음”보다 최신이다.

### N01 [P1] 동시에 자동 시작하면 정상 서비스가 있어도 한 클라이언트가 실패 — 수정·검증 완료

- 위치: `apps/cli/src/service-lifecycle.ts:64`, `:101`.
- 서비스가 없는 상태에서 두 `ensureService`가 동시에 실행되면 둘 다 데몬을 시작한다. 한 데몬이 데이터 잠금 경쟁으로 종료되면 `startService`가 reject하고, 호출자는 다른 데몬의 준비 상태를 다시 확인하지 않고 실패한다.
- 임시 데이터 폴더와 임의 loopback 포트에서 실제 `Promise.allSettled([ensureService(options), ensureService(options)])`를 실행했다. 결과는 한쪽 fulfilled, 다른 쪽 rejected(`MCPex 백그라운드 서비스 시작 실패: exit 1`)였고 이후 같은 주소의 health는 HTTP 200이었다. 테스트 데몬만 종료했다.
- 영향: 설정 열기와 MCP 연결 또는 여러 클라이언트의 최초 연결이 겹치면 정상 서버가 떠 있어도 일부 연결이 실패한다.
- 수정 기준: 시작 프로세스 종료와 대상 서비스 준비를 분리해서 판단하고, 경쟁 패배 시에도 제한 시간 안에 같은 서비스의 정상 준비를 확인하면 재사용한다. 실제 시작 실패나 다른 서비스 응답은 계속 오류로 처리한다. 동시 시작 회귀 테스트가 필요하다.
- 2026-09-18 수정: 백그라운드 자식의 exit 오류만 경쟁 패배 가능 상태로 보존하고, 기존 시작 제한 시간 안에서 동일 주소의 MCPex health가 준비되면 정상 재사용한다. spawn 오류·명시적 시작 오류·다른 서비스 응답은 즉시 실패하고, 끝까지 준비되지 않으면 원래 exit 오류를 반환한다.
- 검증: 의존성 주입 회귀 테스트에서 동시 두 호출의 한 start가 exit 1로 실패해도 둘 다 완료되는 것을 확인했다. 빌드 산출물을 임시 데이터 폴더·임의 loopback 포트에서 실제로 동시에 두 번 실행해 두 결과 fulfilled, health HTTP 200을 확인하고 테스트 서비스와 임시 데이터를 정리했다.

### N02 [P2] 전체 접근 도구의 경로 요구와 공개 스키마가 불일치 — 수정·검증 완료

- 위치: `apps/server/src/index.ts:2377`, `packages/tools/src/index.ts:413`, `:478`.
- full 모드에서도 scoped 모드의 `workspaceToolDefinitions`를 그대로 제공한다. `list_files.path`와 `run_command.cwd`가 선택 항목이고 절대 경로 설명도 없지만, 구현은 누락값을 `.`로 바꾼 뒤 full 모드에서 거부한다.
- 빌드 산출물의 `new WorkspaceTools(null, {}, [{ commandId: 'node', executable: process.execPath }])`로 `executeWorkspaceTool(tools, new Set(['list_files']), 'list_files', {})`를 호출하면 `PATH_FORBIDDEN`이다. `run_command`에 `{ commandId: 'node', args: ['--version'] }`를 전달해도 동일하다. 둘 다 제공된 스키마상 필수 항목을 만족하며 실제 파일/명령 작업 전 실패했다.
- 영향: 모델이 스키마대로 인자를 작성해도 실행이 실패한다. 서버 도구 루프는 이 오류를 다시 모델에 수정 요청하지 않고 실행 실패로 전달한다. UI의 절대 경로 안내만으로 모델 계약이 보완되지는 않는다.
- 수정 기준: 정책에 맞는 도구 스키마·설명으로 full 모드의 path/cwd 절대 경로 필수를 전달하거나 명확한 기본 폴더 계약을 도입한다. fixed/caller의 상대 경로 기본 동작은 유지한다. 실제 모델에 전달되는 도구 정의와 실행기를 함께 검증한다.
- 2026-09-18 수정: 기존 workspace 도구 정의를 기준으로 full 실행에서만 모든 파일 `path`와 `run_command.cwd`에 절대 경로 설명을 추가하고 required로 공개한다. fixed/caller는 기존 정의와 `list_files.path`·`run_command.cwd` 생략 시 작업 폴더 기본값을 그대로 유지한다.
- 검증: scoped/full 정의의 required 차이와 설명을 단위 검사하고, 절대 경로 `list_files`와 `run_command` 실행을 확인했다. 서버 통합 모의 모델 요청에서도 full `list_files.path`가 required이고 절대 경로 설명이 전달되며, 모델이 보낸 절대 경로 호출이 실제 실행된 뒤 완료되는 것을 확인했다.

### N03 [P2] `..`로 시작하는 실제 하위 폴더가 잠금 충돌 검사에서 제외 — 수정·검증 완료

- 위치: `packages/runtime/src/index.ts:27–35`.
- `overlaps`가 `startsWith('..')`로 상위 이동 여부를 판단해 합법적인 하위 폴더명 `..cache`도 상위 이동처럼 처리한다.
- `WorkspaceLockManager.run('C:\\review-root', pendingTask)`로 부모 잠금을 유지한 상태에서 `canRun('C:\\review-root\\child')`는 false, `canRun('C:\\review-root\\..cache')`는 true였다. 순수 잠금 객체로 재현했으며 실제 파일을 변경하지 않았다.
- 영향: 부모 프로젝트와 해당 하위 프로젝트의 파일 변경 작업이 동시에 실행될 수 있다. 전체 접근 전역 잠금과 별도로 fixed/caller의 경로 중첩 보장이 깨진다.
- 수정 기준: 상대 경로가 정확히 `..`이거나 `..` 다음 경로 구분자로 시작할 때만 부모 이동으로 판정한다. 양방향 포함·독립 폴더·`..cache` 하위 폴더를 검증한다.
- 2026-09-18 수정: 상대 경로가 정확히 `..`이거나 `..${path.sep}`로 시작할 때만 상위 이동으로 판정한다. 이름 자체가 `..`로 시작하는 `..cache` 등은 정상 하위 경로로 처리한다.
- 검증: 부모 잠금 중 일반 자식과 `..cache` 자식은 모두 차단되고 독립 경로는 허용되는지 확인했다. 반대로 자식 잠금 중 부모도 차단해 양방향 포함 관계를 검증했다.

### 검증 결과와 한계

- `npm run build`, `npm run typecheck`, `npm run lint`: 통과.
- `npx vitest run --maxWorkers=2`: N01–N03 회귀 추가 후 21개 파일·67개 테스트 통과.
- `npm run test:e2e`: 최초 검토에서는 Edge 기능 시나리오 통과 뒤 셸 자식으로 남은 `node tests/e2e/server.mjs` 때문에 수동 PID 종료가 필요했다. 2026-09-18 Playwright `globalSetup`/teardown이 서버를 직접 소유하도록 변경한 뒤 연속 2회 실행이 각각 1 passed, exit 0으로 무인 종료됐고 포트 listener도 정리됐다. 종료 지연은 해결했다.
- 외부 네트워크 연결은 화면에 미지원으로 분리되어 있다. 실제 사용자 Codex 영구 등록·실모델 호출·다중 드라이브/UNC·실제 ACL/junction·전체 백업 복구는 이번에도 검증하지 않았다.
- 사용자 개인 설정과 기본 서비스는 변경하지 않았고 외부 모델 비용이나 사용자 파일 변경은 발생시키지 않았다. 이번 변경의 GitHub 업로드는 수행하지 않았다.

## 2026-09-15 실사용 open 인증 장애 수정

실행 중 서버의 메모리 토큰과 현재 DPAPI 토큰이 달라 bootstrap 401이 지속되는 문제를 확인했다. 암호화 토큰 revision 변경 시 서버 캐시를 갱신하도록 수정했으며, 토큰 교체 회귀 테스트와 실제 기본 데이터의 bootstrap→세션 교환→관리 API가 모두 200으로 통과했다. 토큰을 교체한 외부 원인의 재현, 브라우저 실행과 세션 만료 후 재접속은 별도 검증 항목으로 유지한다.

## 2026-09-15 수정 후 독립 재확인

이번 검토는 V01~V04 변경 코드와 회귀 테스트에 집중했다. 네 항목 모두 수정 반영을 확인했으며 해당 범위에서 추가 차단 문제는 발견하지 않았다. 전체 서비스의 모든 실환경 검증이 완료됐다는 뜻은 아니다.

- V01: 브리지가 io.mcpex/workspace를 명시적으로 전달하며 다른 메타데이터는 전달하지 않는다. 인메모리 SDK 회귀 테스트 통과.
- V02: 큐 오류의 cause 보존과 서버의 cause chain telemetry 조회가 연결됐다. 파일 변경 뒤 timeout MCP 결과의 관측 및 usage 보존 테스트 통과.
- V03: discover-models/probes가 공통 timeout·연결 종료 signal을 adapter에 전달한다. 무응답 모의 공급업체에서 두 경로의 504 PROVIDER_TIMEOUT 테스트 통과.
- V04: 현재 provider 구성원으로 그룹 제한을 재계산한다. 제한 상향·그룹 이동·삭제 회귀 테스트 통과.
- 새 build 완료 후 npm test 재실행: 15개 파일·40개 테스트 통과. build/typecheck(web 포함)/lint 통과.
- Microsoft Edge E2E 시나리오 1개 통과. 단, 시나리오 완료 뒤 runner가 테스트 서버 정리에서 종료되지 않아 이번 실행이 생성한 테스트 서버 PID만 수동 종료했다. 이후 Playwright는 1 passed, exit 0으로 종료됐다. 따라서 기능 시나리오는 통과지만 무인 E2E 종료·정리는 추가 확인이 필요하다. 원인은 이번 검토에서 확정하지 않았다.
- 기존 사용자 서버를 종료하지 않았으며 제품 코드 수정 없이 검토·검증 문서만 갱신했다. 실제 사용자 MCP 클라이언트·실제 클라우드 모델·전체 복구 검증은 여전히 별도다.

## 2026-09-15 재검토 — 최신 판단

R01~~R12 대응은 대부분 실제 코드에 반영됐다. 인증 hook, 실행 도구 허용 검사, UI·관리 API, 입력·출력 스키마, 큐/취소, 스냅샷, 수집 단계 출력 제한을 확인했다. 후속 V01~~V04도 모두 수정·검증했으며 이번 재검토에서 확인한 코드 연결 문제는 남아 있지 않다. 아래 기존 9월 13일 문제 설명은 당시 코드의 이력이다.

### V01 · P1 · STDIO 브리지가 caller workspace 메타데이터를 전달하지 않음 — 수정·검증 완료

- 위치: apps/cli/src/tool-bridge.ts:58; apps/server/src/index.ts:2599.
- 브리지는 name/arguments와 signal만 전달한다. 서버의 caller workspace는 `context.mcpReq._meta['io.mcpex/workspace']`에서 읽으므로 이 값을 준 상위 호출도 STDIO를 거치면 WORKSPACE_REQUIRED가 된다.
- 인메모리 SDK client→실제 ToolCatalogBridge→모의 backend 호출로 확인: 입력 메타데이터의 작업 폴더가 backend에서 null이었다. 실제 폴더 접근은 하지 않았다.
- 2026-09-15 수정: 브리지가 문자열 `_meta["io.mcpex/workspace"]`만 새 메타데이터 객체로 백엔드 호출에 전달한다. 상위 요청의 다른 임의 메타데이터는 중계하지 않는다.
- 공식 SDK 인메모리 client→실제 ToolCatalogBridge→모의 backend 테스트에서 사용자 정의 workspace 값 전달과 비허용 메타데이터 제거를 확인했다. 실제 별도 STDIO 프로세스 검증은 계속 미검증이다.

### V02 · P1 · 취소·deadline 오류 변환 중 변경 관측 정보 소실 — 수정·검증 완료

- 위치: packages/runtime/src/index.ts:225~231; apps/server/src/index.ts:2309,2629.
- executeAgent가 오류 객체에 executionTelemetry를 넣지만 큐가 취소/deadline을 새로운 QueueError로 바꾸면서 이를 버린다. MCP 실패 envelope는 그 결과 toolCalls=0, changes=[], checks=[]로 돌아간다. 앞서 파일 편집을 수행한 작업도 취소 결과에서 관측된 변경이 없는 것처럼 보일 수 있다.
- 실제 RunQueue에 가짜 관측 정보가 붙은 오류를 전달하고 취소한 모의 검사에서 code=CANCELLED, telemetryPreserved=false를 확인했다. 파일은 수정하지 않았다.
- 2026-09-15 수정: RunQueue가 실행 중 취소·deadline 오류를 정규화할 때 원래 오류를 표준 `cause`로 보존하고, 서버는 최대 깊이와 순환 방어를 둔 cause chain에서 executionTelemetry를 찾는다.
- 단위 검사에서 실행 중 caller 취소의 원인 telemetry 보존을 확인했다. MCP 통합 검사에서는 read→write 후 다음 모델 요청을 timeout시켜 `timed_out` envelope에 실제 toolCalls 2회, write 변경 경로, 완료된 모델 2턴 usage가 유지됨을 확인했다.

### V03 · P2 · 프로바이더 시험·모델 목록 조회에는 timeout 미적용 — 수정·검증 완료

- 위치: apps/server/src/index.ts:261,1510,1700.
- 에이전트 실행은 requestTimeoutMs를 signal에 연결하지만 discover-models와 models/:id/probes는 signal 없이 adapter를 호출한다. 해당 provider의 제한 시간을 짧게 설정해도 UI 시험/조회에는 적용되지 않으며 응답 없는 서버 때문에 요청과 종료 정리가 대기할 수 있다.
- 정적 검토로 확인. 외부 무응답 서버 재현은 하지 않았다.
- 2026-09-15 수정: 두 관리 API가 provider의 `requestTimeoutMs`와 실제 HTTP 연결 종료를 결합한 공통 signal을 adapter fetch에 전달한다. 제한 시간 만료는 HTTP 504와 `PROVIDER_TIMEOUT`으로 구분한다.
- 무응답 로컬 HTTP provider를 사용해 모델 목록 조회와 probe가 설정된 제한 시간에 504로 종료됨을 확인했다. 같은 회귀 테스트의 에이전트 timeout과 UI 취소도 유지됐다.

### V04 · P2 · resource group 제한을 낮춘 뒤 올릴 수 없음 — 수정·검증 완료

- 위치: packages/runtime/src/index.ts:102~~114; apps/server/src/index.ts:1049~~1058.
- setResourceGroupLimit이 과거 값과 새 값의 최솟값만 저장한다. 그룹의 현재 provider 설정을 1→2로 변경하거나 제한이 가장 낮은 provider를 그룹에서 제거해도 이전 제한 1이 남는다.
- 실제 RunQueue에 그룹 제한 1→2 적용 후 독립된 모의 작업 두 개를 접수한 검사에서 최대 동시 실행은 1이었다.
- 2026-09-15 수정: 단일 그룹 setter는 전달받은 현재 제한으로 덮어쓰고, 전체 제한 맵을 교체하는 API를 추가했다. 서버는 저장된 현재 provider 전체에서 그룹별 최솟값을 다시 계산해 시작·설정 가져오기·provider 생성·수정·삭제 후 큐에 반영한다. 실행 접수 시 과거 snapshot 제한을 다시 누적하지 않는다.
- 실제 관리 API와 두 provider의 동시 실행을 사용해 구성원 최솟값 1, 1→2 상향, 그룹 이동 후 이전 그룹 2, 새 그룹의 최솟값 1, 제한 provider 삭제 후 2를 확인했다.

### 이번 검증 결과와 한계

- `npm test`: 15개 파일, 40개 테스트 통과.
- `npm run build`, `npm run typecheck`(web 포함), `npm run lint`: 통과.
- 추가 인메모리 검사 3개: workspace metadata 누락, 취소 telemetry 소실, group 제한 상향 미반영 확인.
- 후속 V01 보완 검사: 공식 SDK 인메모리 브리지에서 workspace metadata 전달과 다른 메타데이터 제거 통과.
- 후속 V02 보완 검사: 취소 오류 cause 보존과 파일 변경 후 timeout MCP telemetry·usage 보존 통과.
- 후속 V03 보완 검사: 무응답 provider의 모델 목록 조회·probe 제한 시간과 `PROVIDER_TIMEOUT` 응답 통과.
- 후속 V04 보완 검사: provider 제한 상향·그룹 이동·삭제 때 현재 구성원 기준 resource group 제한 재계산 통과.
- `npm run test:e2e`: `127.0.0.1:47931/health` 기존 점유로 시작 실패. 다른 프로세스를 종료하거나 reuseExistingServer 설정을 바꾸지 않았다. 이전 E2E 통과 기록은 유지하지만 이번 회차 재통과로 계산하지 않는다.
- 실제 클라우드 모델·실제 사용자 STDIO 클라이언트·설정 파일 UI 이동·전체 백업 복구는 이번에도 미검증.
- V01~V04 제품 코드와 회귀 테스트를 보완했다. 이 최신 절과 이전 검토의 역사적 설명을 구분한다. 아래의 과거 '브라우저 상호작용 미수행' 문구는 9월 13일 검사 범위이며 이후 E2E 수행 이력을 부정하지 않는다.

검토일: 2026-09-13. 대상: 현재 작업 트리 전체. 모든 코드가 untracked 상태이므로 특정 커밋 diff가 아닌 현재 파일 기준이다. 제품 소스 수정은 하지 않았다.

## 판정

모의 환경의 기본 수직 흐름은 동작하지만 설계한 v1 구현 완료로 판단할 수 없다. P0~P4에도 연결되지 않은 필수 기능이 있다. 아래는 소스 정적 검토에서 확인한 문제다. 보안 문제의 악용 재현이나 외부 서비스 요청은 수행하지 않았다.

## 후속 조치 상태

2026-09-14까지 권장 순서의 R01부터 R12까지 수정하고 자동 검증했다.

- **R01 수정·검증 완료:** DPAPI 보호 로컬 접속 토큰을 생성·재사용하고 `/auth/bootstrap`과 `/mcp`에 Bearer 인증을 적용했다. UI 교환 세션을 서버에서 만료 시간과 함께 관리하며 모든 관리 API에 세션 또는 로컬 토큰 인증을 적용했다. localhost Host 허용 목록과 세션 기반 쓰기 요청의 동일 Origin·CSRF 헤더 검사를 추가했다.
- **R02 수정·검증 완료:** 실행 dispatcher가 에이전트의 활성 도구 집합을 다시 검사하고 workspace 도구별 필수 인자·형식·추가 인자를 검증한다. `runtime.mode=tools`일 때만 도구를 제공하며 MCP readOnly/openWorld annotation을 실제 활성 도구 정책에 맞췄다.
- **R03 구현 완료·부분 검증:** Vite 개발 proxy, 서버의 제한된 정적 파일 제공과 SPA fallback, 실제 출력 위치를 사용하는 CLI bin, `mcpex open`의 bootstrap 발급·fragment URL 브라우저 실행을 구현했다. 정적 파일·경로 이탈 테스트와 로컬 브라우저 렌더링을 확인했으나 Playwright E2E는 아직 없다.
- **R04 구현 완료·부분 검증:** 프로바이더별 모델 등록과 명시적 시험 모델 선택, 템플릿 기반 에이전트 생성, 모델·프롬프트·입력 스키마·출력·workspace 도구 편집, 초안 저장·미리보기·시험·적용·활성화, 실행 목록 화면과 API를 구현했다. 루트 typecheck 명령에서 web도 `--noEmit`으로 검사한다.
- **R05 수정·검증 완료:** JSON Schema 2020-12 제한 검증기를 추가해 적용 시 input/output schema를 검사하고 API·MCP 실행 입력에 강제했다. HTTP MCP와 STDIO bridge는 실제 inputSchema를 등록하며, text/Markdown/JSON 출력 형식과 JSON 스키마, 128KiB 출력 상한을 검사한다. 실패 시 `INVALID_INPUT`, `INVALID_SCHEMA`, `INVALID_OUTPUT`, `OUTPUT_LIMIT`을 구분하고 제한된 원문을 run에 보존한다.
- **R06 수정·부분 검증 완료:** 실행 접수 시 절대 deadline을 정하고 대기·실행 단계에서 만료를 적용한다. UI 취소 API와 MCP 요청 signal을 queue→runtime→provider fetch·workspace tool까지 전달하며 caller 취소와 timeout을 각각 `cancelled`/`timed_out` run으로 기록한다. 공급업체별 `requestTimeoutMs`도 실제 fetch signal에 연결했다.
- **R07 수정·부분 검증 완료:** CLI와 서버 직접 실행에 SIGINT/SIGTERM graceful shutdown을 연결하고 종료 시 진행 작업 취소·정리 후 DB와 lock을 닫는다. stale PID lock을 회복하고 살아 있는 소유자는 거부하며, 저장소·서버 초기화 실패 시 열린 DB와 lock을 해제한다. 재시작 시 남은 queued/running run은 `interrupted`로 전환한다.
- **R08 수정·검증 완료:** command 설정을 최대 개수·고유 commandId·절대 executable 경로·허용 필드 기준으로 적용/실행 전에 검증하고 `WorkspaceTools`에 전달한다. UI에도 허용 명령 JSON 편집을 연결했으며 서버 모델 반복에서 allowlisted 명령 실행을 확인했다.
- **R09 수정·검증 완료:** 기존·신규 provider의 `maxConcurrency`, `resourceGroup`, `resourceGroupConcurrency`를 큐에 반영하고 provider ID와 resource group을 실행 제한 키로 사용한다. 모델 `defaultGeneration`에 agent override를 덮어쓴 해석 결과를 preview와 실제 adapter 요청에 전달하며 UI에 관련 설정 입력을 추가했다.
- **R10 수정·검증 완료:** STDIO 브리지가 백엔드 `tools/list` 캐시를 주기적으로 강제 갱신하고 도구 추가·정의 변경·삭제를 로컬 MCP 등록에 반영한다. 변경 시 SDK의 `tools/list_changed`를 상위 클라이언트에 전달하며 갱신된 정의와 취소 signal로 호출을 중계한다.
- **R11 수정·검증 완료:** schema v2의 `runs.config_snapshot_json`과 v1 마이그레이션을 추가했다. 적용 버전 생성 시 에이전트·모델·프로바이더 해석 결과를 원자적으로 저장하고, 실행은 접수 시점 스냅샷과 메모리에 고정한 인증값을 사용한다. UI 시험 실행은 완료를 기다리지 않고 `202 queued`를 반환하며 MCP metadata는 현재 초안이 아닌 적용 버전을 사용한다.
- **R12 수정·검증 완료:** 최대 읽기 크기를 사전 확인하고 제한량보다 한 바이트 이상 읽지 않는 공통 파일 읽기를 검색·교체·hash 확인에 적용했다. 검색은 초과 파일을 건너뛰고 목록·검색 결과는 전체 직렬화 바이트 예산을 지킨다. 명령 stdout/stderr는 실행 중 공유 상한까지만 보관하고 전체 관측 바이트와 잘림 상태를 반환한다.
- **검증:** `npm test` 11개 파일·32개 테스트와 build, typecheck, lint 통과. 초과 파일 읽기 거부·검색 제외, 긴 검색 행과 파일 목록의 결과 상한, 대용량 stdout/stderr의 수집 단계 합산 상한을 확인했다. 실제 장시간 대용량 프로세스의 RSS 측정은 미수행이다.
- **P6 구현 완료·부분 검증:** 프로바이더·모델 관리, 템플릿·에이전트 수명주기, 실행 이벤트/SSE, 설정 이동·보존·백업에 이어 API 오류의 상태·code·details와 해결 안내를 표시하는 오류 UI를 구현했다. Playwright E2E는 일회용 fragment 인증부터 provider/model, 서로 다른 템플릿 agent 2개, 충돌 입력 유지, 시험·적용·복수 활성·기록·편집 재적용까지 검증한다.
- **검증:** `npm test` 15개 파일·37개 테스트, Microsoft Edge `npm run test:e2e` 1개 테스트와 build, typecheck, lint 통과. 설정 이동 UI 파일 선택, clean install, DB 백업 교체 재기동, 실제 모델·MCP 클라이언트는 별도 미검증 범위다.
- **다음 작업:** 아래 추가 누락의 실행 안전성 항목과 실환경 검증을 순서대로 진행한다.

## 실행한 검사

| 명령              | 결과                              |
| ----------------- | --------------------------------- |
| npm test          | 15개 파일, 37개 테스트 통과       |
| npm run build     | 모든 workspace 빌드 통과          |
| npm run lint      | 통과                              |
| npm run typecheck | 통과. web도 별도 `--noEmit` 검사  |
| npm run test:e2e  | Microsoft Edge 1개 수직 흐름 통과 |

실제 클라우드 API, 실제 사용자 MCP 클라이언트, 브라우저 상호작용, clean install, 타 Windows 계정 검증은 수행하지 않았다. 테스트는 기존 테스트만 실행했다.

## 수정 필요 사항

### R01 · P1 · 로컬 인증이 API 및 MCP에 적용되지 않음 — 수정·검증 완료

위치: apps/server/src/index.ts:172~~195, 716~~719.

exchange는 쿠키를 발급하지만 세션을 저장하거나 검증하지 않는다. API/MCP의 인증 hook이 없고 bootstrap 발급도 접근 검증이 없다. Host/Origin/CSRF 검사도 서비스 코드에 없다. 인증 없는 로컬 호출을 사용자 승인으로 취급하면 안 된다. 공통 요청 인증·세션 저장·브리지 인증 및 로컬 HTTP 보호를 구현하고 모든 관리/실행 경로에 적용해야 한다.

### R02 · P1 · 선택한 도구 목록이 실행 허용 목록으로 강제되지 않음 — 수정·검증 완료

위치: apps/server/src/index.ts:548~577; packages/runtime/src/index.ts의 runToolLoop; packages/tools/src/index.ts의 executeWorkspaceTool.

enabledTools는 모델에 제공하는 정의만 필터링한다. 반환된 도구 호출은 활성 목록과 인자 스키마 검증 없이 공용 dispatcher로 전달된다. 실행 직전 선택된 도구인지 검사하고 인자를 검증해야 읽기 전용 같은 설정을 보장할 수 있다. runtime.mode도 실제 분기에서 사용하지 않으며 MCP readOnlyHint는 항상 true이다. 정책·모드·annotation을 실제 실행 기능에 일치시켜야 한다.

### R03 · P1 · 정상 실행 경로로 웹 UI에 접근할 수 없음 — 구현 완료·부분 검증

위치: apps/cli/src/index.ts:9~14; apps/web/vite.config.ts:3; apps/server/src/index.ts.

serve가 정적 웹 파일을 제공하지 않고 open은 안내 문자열만 출력한다. Vite를 별도로 실행해도 /api proxy가 없어 프런트엔드의 상대 API 요청이 백엔드로 전달되지 않는다. 개발 proxy와 빌드 정적 제공을 각각 연결하고 open을 구현해야 한다.

### R04 · P1 · 핵심 에이전트 편집 화면과 다중 모델 선택 흐름 미구현 — 구현 완료·부분 검증

위치: apps/web/src/main.tsx:85~~105,119~~124.

에이전트·실행 기록 메뉴에 동작이 없고 렌더링되는 화면은 프로바이더/모델 등록과 응답 시험뿐이다. 템플릿 적용, 시스템 프롬프트 편집, 에이전트 활성화를 UI에서 할 수 없다. 모델 등록과 시험 대상도 providers[0]/models[0]으로 고정되어 두 번째 연결·모델을 선택할 수 없다. 이는 단순 미검증이 아니라 사용자 요구의 미구현이다.

### R05 · P1 · 사용자 입력·출력 스키마가 실행에 반영되지 않음 — 수정·검증 완료

위치: apps/server/src/index.ts:587~588,665; apps/cli/src/index.ts:27.

MCP 서버와 브리지는 입력을 임의 record로 등록해 필수 항목·설명이 도구 목록에 전달되지 않는다. 시험 실행도 inputSchema 검증이 없다. 출력은 설정과 무관하게 Markdown completed로 저장해 JSON 출력 형식 실패를 탐지하지 못한다. 적용 시 스키마 검증, MCP 전달, 호출 시 입력 검사, 완료 시 출력 검사를 연결해야 한다.

### R06 · P1 · 취소 및 시간 제한이 실제 모델 작업에 전달되지 않음 — 수정·부분 검증 완료

위치: apps/server/src/index.ts:570~~580,618~~623,668~679.

queue callback은 제공된 signal을 받지 않고 executeAgent도 signal 인자가 없다. runToolLoop와 응답 호출에는 상위 signal이 전달되지 않는다. timeoutMs/requestTimeoutMs를 deadline에 연결하지 않으며 취소 API도 없다. 공급업체가 응답하지 않으면 작업·슬롯이 계속 점유될 수 있다. 접수 시 deadline 설정 및 MCP/UI→queue→runtime→fetch/tools 취소 전파가 필요하다.

### R07 · P1 · CLI 종료 후 데이터 디렉터리 잠금이 남음 — 수정·부분 검증 완료

위치: apps/cli/src/index.ts:9~~12; packages/storage/src/index.ts:23~~39.

CLI는 createServer의 close를 버리고 SIGINT/SIGTERM 종료 처리를 등록하지 않는다. 파일 잠금은 명시적 release에서만 삭제되며 기존 lock 파일의 소유 프로세스 생존 확인도 없다. 종료나 비정상 종료 후 같은 디렉터리로 재시작하면 DATA_DIR_LOCKED가 유지된다. graceful shutdown과 stale lock 판별·안전한 회복을 구현해야 한다. 부트스트랩 실패 시 획득한 잠금 해제도 필요하다.

### R08 · P2 · 명령 허용 설정이 도구 인스턴스에 전달되지 않음 — 수정·검증 완료

위치: apps/server/src/index.ts:551; packages/tools/src/index.ts:52,209~210.

WorkspaceTools를 workspace만 전달해 생성하므로 commands는 항상 빈 배열이다. 에이전트에 명령을 설정해도 실제 run_command는 허용된 commandId를 찾지 못한다. 설정을 검증한 뒤 생성자에 전달해야 한다. 직접 WorkspaceTools를 생성하는 단위 테스트 성공은 서버 연결 성공을 의미하지 않는다.

### R09 · P2 · 프로바이더 동시성 및 모델 기본 생성 설정이 무시됨 — 수정·검증 완료

위치: apps/server/src/index.ts:164,558~560,623,679.

RunQueue는 기본 전역 제한만 사용하고 설정된 providerLimits/resourceGroup을 넣지 않는다. provider 키에도 provider ID가 아닌 modelRef를 사용한다. maxConcurrency=1로 설정해도 같은 공급업체의 다른 모델 작업이 함께 실행될 수 있다. defaultGeneration도 실행 시 읽지 않고 generationOverrides만 전달한다. 모델 기본값 해석과 공급업체/그룹 제한을 공통 설정 해석 단계에 연결해야 한다.

### R10 · P2 · STDIO 연결 이후 활성 목록 변경이 반영되지 않음 — 수정·검증 완료

위치: apps/cli/src/index.ts:23~33.

브리지는 연결 시 listTools를 한 번 호출해 등록하고 이후 목록 변경 알림·재조회·등록 해제를 처리하지 않는다. 사용자가 새 에이전트를 활성화해도 기존 STDIO 연결에서 찾을 수 없다. 백엔드 catalog 변경과 브리지 목록 동기화를 구현하거나 재연결 필요를 명시해야 한다.

### R11 · P2 · 적용 버전 및 실행 기록이 재현 가능한 스냅샷이 아님 — 수정·검증 완료

위치: apps/server/src/index.ts:460~~468,520~~536,664.

적용은 초안 JSON만 저장하고 실행 시 현재 모델/프로바이더를 다시 조회한다. 모델·프로바이더 설정의 적용 시점 스냅샷이 없으며 MCP description은 적용 버전 대신 현재 초안에서 읽는다. runs에도 configSnapshot이 없다. 실행 슬롯을 얻기 전에는 run이 생성되지 않아 대기 상태 조회가 불가능하고 test-runs는 완료까지 기다린 후 202를 반환한다. 적용 버전·접수 시 스냅샷·비동기 접수 및 상태 기록을 분리해야 한다.

### R12 · P2 · 도구의 출력·읽기 제한이 메모리 사용을 제한하지 않음 — 수정·검증 완료

위치: packages/tools/src/index.ts:138~~145,222~~238.

검색은 목록의 파일을 크기 검사 없이 통째로 읽고 반환 행의 바이트 제한도 적용하지 않는다. 명령 stdout/stderr는 종료까지 무제한 문자열에 누적한 뒤 마지막에 자른다. 큰 로그·파일을 정상 처리할 때 메모리 고갈과 모델 문맥 초과가 발생할 수 있다. 읽기 전 크기 검사, 수집 단계 버퍼 상한, 결과 전체 바이트 한도를 적용해야 한다.

## 추가 누락 및 문서 상태

- provider/model 관리, 사용자 템플릿 CRUD/적용, 에이전트 삭제·복제, 실행 SSE, 설정 이동·보존·백업, 오류 UI와 핵심 브라우저 E2E는 구현했다. 설정 이동 UI의 파일 선택과 실제 외부 연동은 남아 있다.
- **수정·검증 완료:** 큐가 실행 가능 작업을 선택할 때 workspace 잠금 충돌도 검사하므로 잠금 대기 작업은 전역 실행 슬롯을 차지하지 않는다. 독립 workspace 작업이 먼저 실행되는 회귀 테스트를 추가했다.
- **수정·검증 완료:** workspacePolicy.caller를 UI 시험 실행 본문의 별도 workspace 필드와 MCP `_meta["io.mcpex/workspace"]`에 연결했다. 절대 경로·사전 허용 루트 포함 관계를 검증하고 실제 경로를 실행 스냅샷에 고정한다.
- **수정·검증 완료:** allowlist 명령과 Windows 종료 헬퍼는 부모 `process.env` 대신 플랫폼별 최소 OS 환경만 전달한다. 임의 부모 비밀 변수와 `NODE_OPTIONS`가 자식 Node 프로세스에 없고 `PATH`는 유지되는 회귀 테스트를 추가했다.
- **수정·검증 완료:** `write_file`은 대상 존재 여부를 항상 확인해 기존 파일에는 `expectedHash`를 강제하고, `replace_text`는 도구 스키마와 실행기 모두에서 해시를 필수화했다. 누락·불일치 시 기존 내용이 유지되고 최신 읽기 해시로만 수정되는 테스트를 추가했다.
- **수정·검증 완료:** MCP 성공·애플리케이션 실패 응답은 공통 envelope를 `structuredContent`와 JSON 텍스트에 동일하게 제공한다. 실제 도구 호출·변경·명령 검사·잘림을 집계하고, 전체 모델 턴 usage와 큐 접수부터 완료까지의 duration을 반환한다. 도구 observation과 모델별 usage는 실행 이벤트에도 남긴다.
- **수정·검증 완료:** UI와 적용 버전 MCP 도구 설명에 파일 결과의 모델 제공자 전송·클라우드 외부 전송 가능성과 허용 명령의 현재 OS 사용자 권한·비-sandbox 실행을 실효 권한 기준으로 표시한다.
- Playwright 의존성·설정·수직 테스트와 web 전용 TypeScript 검사를 추가했다.
- 기존 문서의 P0~P4 완료 및 CRUD 표현은 위 결과에 따라 부분 구현으로 재평가해야 한다. 후속 API 어댑터 수보다 사용자 수직 흐름과 공통 실행 계약 완성이 우선이다.

## 권장 수정 순서

R01/R02 실행 경계 → R03/R04 사용자 흐름 → R05/R06/R07 실행 계약·수명주기 → R08~R12 연결 누락 → P6 운영 기능 및 E2E. 각 수정은 해당 거부·실패·종료 경로를 검증하고 기존 16개 성공 테스트만으로 완료 판단하지 않는다.
