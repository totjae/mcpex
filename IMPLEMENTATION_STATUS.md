# 구현 상태

## 2026-09-25 게시 전 독립 작동 확인 — R14/R15 해결 확인

빌드/typecheck/lint, Windows OS 전용 시험을 포함한 전체 109개 회귀, Edge E2E 1개, 임시 서비스 시작·재시작 시험을 통과했다. R15 잠금 생성 경합·프로세스 간 저장과 R14 실제 후손 추적을 확인했다. R13의 반복 5초 timeout은 해당 통합 시험만 15초로 조정해 재검증했다. 제품 실행 시간 설정과 실제 사용자 서비스·DB는 변경하지 않았다. 외부 모델·클라이언트 및 운영 부하 전체가 검증된 것은 아니다. 상세는 TEST_PLAN.md를 따른다.

## 2026-09-25 R15 — 구현 완료, 임시 환경 검증

전체 빌드·typecheck·lint 및 Vitest 108개 통과(기존 opt-in OS 시험 1개 제외). UI 변경이 없어 Edge E2E는 이번 단계에서 재실행하지 않았다.

비밀 저장의 작성 중 잠금을 다른 요청이 빈/부분 JSON이라는 이유로 삭제하던 경로를 제거했다. 자기 소유 토큰이 정확히 일치할 때만 해제하며, 중단된 작성자나 소유권 불명 잠금은 10초 후 `SECRET_STORE_LOCKED`로 실패하고 서비스·CLI 중지 후 수동 복구한다. 임시 데이터에서 결정적 생성 경합, 같은 프로세스 및 별도 프로세스 set/set·set/delete, 작성자 중단·시간 초과·복구 시험을 통과했다. 실제 사용자 서비스·DB·비밀 파일은 미변경·미검증이다. 상세 전체 회귀 결과는 TEST_PLAN.md를 따른다. 아래 R15 추가 수정 필요 문구는 수정 전 독립 검토 이력이다.

## 2026-09-25 전체 독립 확인 — R14 해결, RTA-02 추가 수정 필요

R14 후손 추적·영속화·자동 해제 금지와 회귀를 확인했다. 빌드/typecheck/lint, Vitest 102개, Edge E2E 1개 통과. 실제 OS 전용 1개는 현재 환경 조회 불가로 제외했다. 별도로 비밀 저장 잠금 생성 직후의 빈 파일을 경쟁 요청이 삭제해 두 저장 중 한 키가 유실되는 R15를 임시 데이터·결정적 I/O 순서 주입으로 재현했다. RTA-02는 추가 수정이 필요하며 [검토 보고서](docs/IMPLEMENTATION_REVIEW.md)와 [시험 기록](TEST_PLAN.md)에 근거·인수 기준을 남겼다. 제품 코드와 실제 사용자 서비스·DB는 변경하지 않았다.

## 2026-09-25 R14 수정 — 구현 완료, 임시 환경 검증

빌드·typecheck·lint·Edge E2E 통과. 전체 회귀 재실행 102개 통과, 실제 Windows opt-in 시험은 별도 실행 통과. 첫 전체 회귀의 기존 target-binding 5초 timeout 및 상세 검증 범위는 TEST_PLAN.md에 기록했다.

모든 저장된 조상으로 후손 추적을 확장했고 새 식별자를 종료 실패 기록·API 확인 결과에 영속 보존한다. PID 재사용 시 새 세대의 후손을 기존 트리에 편입하지 않는다. 실패 차단은 빈 조회 결과만으로 해제하지 않으며 독립 OS 확인 후 명시적 수동 해제만 허용한다. 실제 임시 Windows 3세대 프로세스에서 부모·자식 종료 후 손자 탐지를 확인했고, 모의 OS 목록 + 임시 DB/API 시험으로 반복 후손 발견·재시작 보존·해제 거부/수동 해제를 확인했다. 실제 사용자 서비스·DB는 미변경·미검증이다. 아래 추가 수정 필요 표시는 수정 전 검토 이력이다.

## 2026-09-25 RTA-04 후속 검토 — 추가 수정 필요

차단·영속화·명시적 해제 구현 및 관련 13개 회귀/빌드는 확인했다. 다만 기록된 자식이 새 손자를 만든 뒤 종료하면 `survivingTree`가 생존 손자를 놓치는 합성 입력을 재현했다. 종료 재확인 API가 차단을 잘못 해제할 수 있으므로 RTA-04 완료 조건은 아직 충족하지 않는다. [전체 구현 검토 R14](docs/IMPLEMENTATION_REVIEW.md)의 추적 보완과 API·재시작 경계 검증이 필요하다. 아래 전체 검사 통과 기록은 기존 수행 이력이며 이 누락의 해결을 의미하지 않는다.

## 2026-09-25 런타임 개선 RTA-01–09 종합 — 구현 완료, 부분 검증

인계 문서의 9개 항목을 지정 순서대로 구현하고 관련 회귀 시험을 통과시켰다. RTA-04의 종료 확인 실패 후 추가 실행 차단·재시작 보존·명시적 해제도 후속 구현했다. 최종 build/typecheck/lint, Vitest 32개 파일·101개 시험, Edge E2E 1개가 통과했다. 실제 사용자 서비스·DB·비밀값은 사용하거나 재시작하지 않았다. 실환경 부하·프로세스 조회 권한/경합 등 각 절의 미검증 사항은 남아 있으므로 전체를 검증 완료로 보지 않는다.

## 2026-09-25 RTA-09 세션 정리 — 구현·자동 검증

bootstrap 교환 시 만료 세션을 Map에서 제거하고 같은 브라우저가 제시한 기존 세션을 새 세션으로 대체한다. 다른 브라우저의 유효 세션은 유지한다. 가상 시각의 정리와 쿠키 교체·다른 브라우저 인증 시험이 통과했다. 새 교환이 없는 동안 만료 세션은 즉시 수거되지 않는다.

## 2026-09-25 RTA-08 보존 정리 — 구현·부분 검증

만료 terminal 실행의 본문·이벤트를 100건씩 트랜잭션으로 정리하고 배치 사이 이벤트 루프에 양보한다. 시작·설정 변경 시 정리하며 1시간 간격으로 반복한다. 동시 정리를 직렬화하고 종료 시 timer를 제거하고 진행 중 정리를 기다린다. 205개 만료 실행의 100/100/5 배치와 진행 중 실행 보존을 자동 검증했다. 단일 실행의 매우 많은 이벤트 삭제 시간, 설정 변경·종료와 겹치는 정리, 실제 API 지연은 미측정이다.

## 2026-09-25 RTA-05 공급자 수신·대화 예산 — 구현·자동 검증

모든 어댑터의 성공·오류 body에서 실제 디코딩된 수신량을 2 MiB로 제한하고 초과 시 reader를 취소한다. `PROVIDER_RESPONSE_TOO_LARGE`를 관리 API/실행 오류 코드로 구분한다. 도구 루프의 초기·중간 assistant/tool 메시지는 8 MiB 누적 예산을 초과하면 `CONVERSATION_LIMIT`로 종료한다. 거짓 Content-Length·멀티바이트·누적 반복 시험과 공급자 회귀가 통과했다. 실제 압축·취소 중 수신 시험은 미검증이다.

## 2026-09-25 RTA-06 디렉터리 열거 — 구현·자동 검증

list_files/search_text의 목록 기반 탐색을 `opendir` 비동기 iterator로 전환해 결과 생성 전에 전체 Dirent 배열을 만들지 않는다. 탐색 항목 10,000개·깊이 20 상한과 취소 검사를 적용하고 기존 출력 제한·제외 폴더를 유지한다. 항목 예산 초과·취소·핸들 닫힘 및 도구 회귀 24개 통과. 매우 큰 합성 폴더의 RSS 전후 측정은 미검증이다.

## 2026-09-25 RTA-01 스키마 검증기 캐시 — 구현·자동 검증

Ajv 설치 구현의 `removeSchema(object)`가 객체 캐시를 해제함을 확인하고 입력·출력 검증과 저장 전 검사에 공통 내용 기반 LRU 캐시를 적용했다. 최대 64개·직렬화 키 총 512 KiB로 제한하고 실패 컴파일 뒤에도 Ajv 객체 참조를 제거한다. 같은 내용의 새 객체 재사용, 100개 서로 다른 스키마와 실패 스키마의 보관량 제한, 기존 입력 검증 회귀를 확인했다. 장시간 힙 추세는 미측정이다.

## 2026-09-25 RTA-02 서버·CLI DPAPI 비동기화 — 구현·부분 검증

서버 인증·자격증명 조회/교체/삭제와 CLI open/mcp에서 비동기 DPAPI 경로를 사용한다. PowerShell은 숨김 stdin 입력·10초 timeout·1 MiB 출력 상한을 적용하고 실패 메시지에 원문을 넣지 않는다. secrets.json 쓰기는 별도 잠금과 임시 파일 교체로 직렬화한다. 합성 값의 동시 저장/삭제·초기 토큰 생성 경합 및 전체 87개 회귀가 통과했다. 동기 `getLocalAccessToken`/`DpapiSecretStore`는 기존 테스트용 호환 함수로 남아 있으며 서버·CLI 런타임은 호출하지 않는다. 실제 프로세스 간 동시 쓰기, 보조 프로세스 무응답·과대 출력 모의는 미검증이다.

## 2026-09-25 RTA-07 SSE 오류 격리·배치 전송 — 구현·부분 검증

SSE timer pump의 조회·JSON·쓰기 오류를 연결 단위로 격리하고 응답 close/error에서 timer·drain listener를 정리한다. write(false)에서는 cursor를 보존하고 drain까지 조회를 멈춘다. SSE 조회는 100건씩 나누되 관리 API의 전체 targetChanges·serviceTiers 집계는 기존 무제한 조회를 유지한다. 150건 추가 이벤트의 순서·완전성, terminal 종료와 잘못된 JSON의 timer 오류 후 서버 생존을 자동 검증했다. 실제 느린 TCP 수신자의 backpressure·핸들 수 측정은 미검증이다.

## 2026-09-25 RTA-04 명령 종료 수명 — 차단 보완 구현·부분 실환경 검증

`runCommand`의 정상 완료·시작 오류에서 timer/abort listener를 정리하고 중복 종료 요청을 차단한다. Windows PID·부모 PID·생성 시각으로 프로세스 트리를 수집하고 부모 식별을 다시 확인한 뒤 `taskkill`한다. 성공 뒤 잔존 트리를 확인하며 실패·조회 불가 시 부모 직접 종료만으로 성공 처리하지 않는다. `COMMAND_TERMINATION_FAILED` 실행은 취소 완료가 아닌 실패로 표시한다. 작업 폴더 및 겹치는 경로·전체 접근 작업은 `WORKSPACE_BLOCKED`로 거절하고 차단을 DB 설정에 남겨 재시작 후 복원한다. 관리 화면에서 종료를 재확인해 해제하거나, 조회 불가 시 독립적인 OS 확인 후 명시적으로 수동 해제한다. 시간 기반 자동 해제는 없다. 임시 부모·하위 프로세스의 taskkill 실패·잔존·후속 작업 차단, 정상 취소, PID 생성 시각 변경, 재시작 복원·확인 해제를 자동 검증했다. 권한 허용 Windows에서 실제 트리 종료 시험도 통과했다. 검사와 PID 지정 종료 사이의 잔여 경합, 조회 권한 거부 상태의 운영 절차, DB 기록 실패, 보조 프로세스 무응답은 실환경 미검증이다.

## 2026-09-25 RTA-03 취소 후 후속 작업 차단 — 구현·자동 검증

모델 응답 후·각 도구 전·도구 오류 후 취소를 검사하고, 접수 전/대상 바인딩 중 취소를 큐 제출 전에 처리한다. 파일·대상 도구의 경로 확인, 읽기·검색, 쓰기 반영 직전에 signal을 전달하며 임시 파일은 취소 시 제거한다. 이미 반영된 쓰기는 결과 기록 전에 취소 예외로 바꾸지 않는다. 재현 시험 3개와 관련 회귀 24개, typecheck/lint 통과. 실제 MCP 클라이언트 취소와 쓰기 반영 전후 경합 시험은 미검증이다.

## 2026-09-25 런타임 안정성 개선 — 작업 계획 작성

RUNTIME_AUDIT의 9개 정적 발견 사항을 [런타임 개선 작업 인계](docs/RUNTIME_REMEDIATION_PLAN.md)로 구체화했다. 취소 후 후속 변경 차단을 우선하고 명령 종료·SSE·DPAPI·스키마 캐시·수신량·디렉터리·보존 정리·세션을 순차 개선한다. 현재 문서화만 완료했으며 코드 수정·새 검증은 수행하지 않았다. 원문 위험 추정과 향후 재현·검증 결과를 구분한다.

- **서비스 재시작 실행기 구현·격리 검증 완료:** `MCPex Restart.bat`가 현재 데이터 폴더의 잠금 소유자 PID·시작 시각·실행 파일과 로컬 포트 소유자·health를 확인한 뒤 해당 서비스만 종료하고 숨김 `serve`를 시작한다. 임시 데이터 폴더·포트에서 시작→재시작(PID 변경)→health/schema v7 확인이 통과했다. 실제 사용자 서비스의 종료·재시작은 수행하지 않았다.

## 2026-09-25 UX16 서비스 티어 — OpenAI 직접 연결 구현·모의 자동 검증

OpenAI 공식 Chat Completions 계약에 따라 직접 OpenAI 프로필·엔드포인트에 `auto/default/flex/priority` 모델 기본값과 에이전트 상속·재정의·명시적 필드 생략을 연결했다. DB schema v7의 모델 전용 필드, 적용/실행 스냅샷, 고급 `extraBody.service_tier` 우선순위, 모델 시험·초안 시험·MCP 요청과 요청/실제 처리 티어 기록·UI를 구현했다. 다른 호환 공급자는 지원 미확인으로 표시하고 전용 선택을 막는다. 모의 API에서는 공급자 거부 시 자동 fallback이 없고 응답 티어가 빠지면 확인 불가다. Vitest 80개와 Edge E2E 1개가 통과했으며 UI 저장·새로고침도 확인했다. 실제 OpenAI 계정·모델의 티어 자격, 요금·속도, 다른 공급자 매핑은 미검증이다.

## 2026-09-25 UX15 삭제 에이전트 toolName 재사용 — 구현·자동 검증 완료

DB schema v6에서 `agents`의 전체 이름 유일 제약을 미삭제 행만의 부분 유일 인덱스로 전환했다. 삭제 이력·버전·실행은 이전 ID에 남고, 같은 이름의 새 에이전트는 별도 ID의 비활성·미적용 초안이다. 생성·복제·초안 변경은 DB 제약을, 가져오기 충돌 미리보기는 미삭제 행 조회를 사용하며 삭제 안내와 제품 계약을 갱신했다. 기존 v5 DB의 참조·무결성, 재생성·미삭제 중복·동시 생성·가져오기 및 공식 SDK MCP 클라이언트의 목록 갱신과 새 버전 호출을 자동 테스트했다. Edge E2E에서 삭제 후 동명 재생성과 중복 안내를 확인했다. 실제 사용자 DB의 마이그레이션과 외부 클라이언트 재연결은 미검증이다.

## 2026-09-24 T01 대상 변경 결과 표시 — 구현·자동 검증 완료

성공한 대상 쓰기·치환 이벤트만 관리 API `targetChanges`로 제공하고, 시험 결과와 실행 기록에 대상 ID·작업을 표시한다. 공급자 실패 뒤의 부분 변경도 유지하며 보존 만료는 변경 없음으로 오인하지 않도록 확인 불가로 구분한다. 모의 생성·치환·읽기 전용·부분 실패·새로고침·보존 만료, 새 대상 화면 Edge E2E를 확인했다. 실제 라이트 모델 및 정책·파일 경합 검증은 남아 있다.

## 2026-09-23 대상 파일 지정 — 1차 구현, 일부 검증 완료

메인이 전달한 targets를 MCPex가 검증해 실행별 대상 ID에 연결하는 설계를 [대상 파일 지정 설계](docs/TARGET_FILE_DESIGN.md)에 정리하고 1차 구현했다. `targetBinding=optional`에만 공통 targets 계약을 적용하며 기존 에이전트는 off를 유지한다. 접수 시 경로·중복·도구 권한을 확인해 실행 스냅샷에 고정하고, 대상 실행에서는 ID 기반 도구만 모델과 dispatch에 허용한다. 읽기·새 파일 배타 생성·해시 기반 수정을 재사용하며 기본 코드 구현 템플릿과 시험 UI를 연결했다. 미리보기·모델 메시지에서 자동 대상 경로를 제거하고 변경 결과는 targetId로 식별한다.

1차 구현 당시 build/typecheck/lint, Vitest 24개 파일·78개, 기존 Edge E2E 1개가 통과했다. 새 검증에서 단위·모의 UI/MCP 대상 실행, 대상 경로의 모델·공개 결과 비노출을 확인했다. 이후 T01에서 새 대상 화면 E2E와 결과 연결을 추가 검증했다. 대기 후 경로 변경·경합, 실제 라이트 모델은 미검증이다. 전체 인수 기준을 검증 완료로 표시하지 않는다.

## 2026-09-23 UX13 프로바이더 편집 — 구현·자동 검증 완료

- 기존 프로바이더 카드의 편집 화면을 GET/PATCH와 인증값 교체·제거 API에 연결했다. 이름·프로필·어댑터·로컬/클라우드·URL·동시성·요청 제한·리소스 그룹을 수정하고, 변경 취소·편집 대상 전환 확인·revision 충돌 시 입력 보존과 최신값 조회를 제공한다. 기존 ID와 모델·에이전트 참조는 유지한다.
- API 키는 원문을 불러오지 않으며 유지가 기본이다. 설정 저장 후 키 작업만 실패하면 부분 성공을 표시하고 키 작업을 다시 시도할 수 있다. 고급 headers·extraBody를 보존하고, 별도 모델 조회 결과를 화면에 표시한다. 접수된 실행은 기존 스냅샷 계약에 따라 접수 시점의 설정을 사용한다.
- build/typecheck/lint, 프로바이더 API 회귀와 Edge E2E 통과. 실제 외부 공급자와 사용자 저장 데이터의 수동 확인은 미검증이다.

## 2026-09-23 B01 경로 복구 안내 — 수정·검증 완료

scoped/full 정책과 list_files 허용 여부에 따라 ENOENT/ENOTDIR·PATH_ERROR 안내를 구분했다. full은 존재하는 상위 폴더의 절대 경로 탐색을 안내하고 목록 도구가 없으면 경로 재확인만 안내한다. 실제 루트·원시 오류 경로는 노출하지 않는다. 도구 패키지 빌드, 전체 typecheck/lint, 관련 Vitest 2개 파일·20개 테스트 통과. 기존 실환경 미검증 항목은 그대로 남는다.

## 2026-09-23 독립 재검토 — 수정 전 이력

build/typecheck/lint, Vitest 23개 파일·75개, Edge E2E 1개를 재실행해 통과했다. A01–A07 및 UX10–UX12의 주요 개선 연결을 확인했으나, full 모드 파일 오류가 허용되지 않는 list_files(".") 재시도를 안내하는 B01을 추가 재현했다. 상세 수정 기준은 IMPLEMENTATION_REVIEW를 따른다. 이번 검토에서 제품 코드는 수정하지 않았고 실제 창 깜빡임·클라우드 입출력은 미검증이다.

## 2026-09-23 UX10–UX12 — 구현·자동 검증 완료, 현장 확인 일부 미검증

- UX10: 선택기·모델 목록·삭제 확인을 `모델 표시명 (프로바이더명)`으로 통일했다. 동일 modelId의 프로바이더 구분, label·이름 변경, 이름 누락·중복을 확인했다. 저장 참조 model.id는 유지한다.
- UX11: DPAPI PowerShell 보조 프로세스에 `windowsHide: true`를 적용했다. 제품 내 다른 spawn 경로의 숨김 설정도 대조했다. 실제 사용자 데스크톱의 창 깜빡임·PID 원인은 확인하지 못해 시각적 해결은 미검증이다.
- UX12: 로컬 설정 화면의 실제 기준 폴더·상대 경로 예시를 보강하고, 공통 파일 path·명령 cwd에서 모호한 Windows 경로와 자동 확장하지 않는 표기를 정형 오류로 구분했다. 없는 파일의 원시 절대 경로 메시지 대신 안전한 재탐색 안내를 제공한다. 같은 이름의 다른 파일 자동 선택이나 강제 덮어쓰기는 하지 않는다. 실제 외부 클라우드 모델과 UNC·다중 드라이브 시험은 미검증이다.
- build/typecheck/lint, Vitest 단일 worker 23개 파일·75개 테스트, Edge E2E 1개 통과. 마지막 ENOTDIR 재탐색 보완 후 관련 P4 18개도 다시 통과했다.

## 2026-09-23 A07 실행 완료와 과제 검증 구분 — 구현·자동 검증 완료

- 기존 `completed`와 MCP `outcome=succeeded`는 실행 정상 종료 의미로 유지한다. MCP 결과와 실행 조회에 별도 `verification`을 추가하고, 화면에는 **실행 완료 · 과제 미검증**을 명시한다. 명시적 합격 기준이 없어 현재는 자동 통과/실패를 부여하지 않는다. 실제 명령 종료 코드와 도구 실패 코드는 근거로 표시하며, 도구 실패 후 최종 답변이나 변경 0건을 과제 성공·실패로 오판하지 않는다.
- 공급자 요청 시간 초과 `PROVIDER_TIMEOUT`은 timed_out, 모델 반복 `MODEL_TURN_LIMIT`·도구 횟수 `TOOL_CALL_LIMIT`는 failed로 분리했다. 기존 MCP 필드와 `DEADLINE` 전체 시간 제한은 유지한다. 과거 실행은 소급 판정하지 않는다.
- build/typecheck/lint, Vitest 단일 worker 73/73, Edge E2E 1/1 통과. 외부 모델의 실제 과제 충족 여부와 외부 요청 시간 초과는 미검증이다.

## 2026-09-23 A06 도구 실패 진단 — 구현·자동 검증 완료

- 실패한 `tool.finished` 이벤트에 경로 표기 종류, 허용 루트 기준 상대 대상, expectedHash 제공 여부, 정형 실패 이유를 추가했다. 범위 밖 절대 경로와 full 접근 경로는 대상 문자열을 기록하지 않는다. 원시 인수·해시·파일 내용·예외 메시지는 새 진단에 저장하지 않는다.
- 모의 파일 작업으로 범위 밖 경로, 없는 파일, 해시 누락, 해시 충돌을 구분하고 이벤트에 절대 루트·범위 밖 경로·파일 본문이 없는지 확인했다. build/typecheck/lint, Vitest 단일 worker 72/72, Edge E2E 1/1 통과. 과거 이벤트는 소급 보강하지 않으며, A07과 UX10–UX12는 별도 작업이다.

## 2026-09-23 A02·A04·A05 시간 정책 — 구현·자동 검증 완료

- 기존 `timeoutMs`의 접수 기준 전체 제한은 유지한다. 새 `queueTimeoutMs`·`executionTimeoutMs`를 함께 설정하면 큐 대기와 실제 실행에 별도 제한을 적용한다. 만료 원인을 대기/실행/기존 전체 제한으로 구분하고 취소는 별도로 유지한다.
- 공개 MCP 도구 메타데이터에 서버 최대 대기+15초를 게시하고 STDIO 브리지가 이를 backend 호출 timeout으로 사용한다. 화면에서 시간 정책과 프로바이더 요청당 제한을 초 단위로 설정·확인하고, 실행 기록에 대기·실행·전체 시간 및 현재 대기 원인을 표시한다. DB schema v5가 `started_at`을 보존한다.
- build/typecheck/lint, Vitest 단일 worker 22개 파일·72개 테스트와 Edge E2E 1개 통과. 61초 가상 지연 MCP 응답 및 대기 후 전체 실행 예산, 단계별 만료를 확인했다. 외부 클라이언트 고유 timeout과 실제 공급자 장시간 호출은 미검증이며 MCPex 설정만으로 외부 한도를 늘릴 수 없다.

## 2026-09-23 A01 scope 전달 및 UX12 기본 템플릿 안내 — 구현·자동 검증 완료

- 코드 구현 기본 템플릿의 메시지에 scope를 포함했다. 코드 조사·구현 템플릿의 workspace는 선택 문맥 입력이며 실제 파일 도구 기준을 바꾸지 않는다고 입력 설명과 시스템 메시지에 명시했다.
- 서버가 저장된 builtin 템플릿을 코드 정의와 동기화하고 버전을 올린다. 기존 에이전트의 초안·적용 버전은 자동 수정하지 않으며, 화면에 scope 누락 후보와 적용 버전 여부를 표시한다. 템플릿 차이 보기와 메시지만 초안에 적용하는 경로를 제공한다.
- build/typecheck/lint, Vitest 22개 파일·70개 테스트와 Edge E2E 1개가 통과했다. 실제 사용자 DB의 영향 목록과 외부 공급자 호출은 확인하지 않았다. UX12의 모호한 경로 입력·오류 복구, A02–A07, UX10·UX11은 별도 작업이다.

## 2026-09-21 UX09 scoped 경로 표기 유연화 — 구현·자동 검증 완료

- fixed/caller 작업 폴더 안에서 상대 경로와 범위 내부 절대 경로를 같은 실제 대상으로 처리한다. 파일 도구 path와 `run_command.cwd`가 공통 경계·링크 검사를 사용하며 범위 밖·부모 이동·유사 접두사 형제·junction은 계속 거부한다.
- 실제 설정 root는 공개 MCP 설명과 내부 모델 도구 설명에 주입하지 않는다. 도구 결과의 path도 작업 폴더 기준 상대 표기로 정규화한다. 기존 설정·기본 정책·허용 도구·명령·해시·잠금 정책은 변경하지 않았다.
- build/typecheck/lint, Vitest 22개 파일·69개 테스트와 Edge E2E 1개(11.3초, exit 0)가 통과했다. Windows 대소문자·구분자·공백 경로와 절대 명령 cwd를 자동 검증했다. UNC·실제 다중 드라이브와 NVIDIA HTTP 500은 별도 미검증·미해결 범위다.

## 2026-09-21 파일 입출력 실패 진단 및 모델 경로 안내 보완 (UX09 이전 이력)

- 당시 scoped 도구 설명에는 실제 작업 기준 폴더와 상대 경로 규칙을 전달했다. UX09에서 실제 경로 주입은 제거됐으며, `tool.finished` 실패 이벤트의 오류 코드 저장은 유지해 공급자 오류와 파일 도구 오류를 구분한다. 기존 권한 정책과 사용자 설정은 변경하지 않았다.
- 구현·로컬 검증 완료: 모의 모델과 실제 서버/파일 도구를 연결해 절대 경로 거부, 상대 경로 생성·읽기·해시 기반 수정 및 오류 코드 기록을 확인했다. 전체 빌드/typecheck와 자동 테스트 22개 파일·69개 통과.
- 실제 NVIDIA 연동은 미해결: 고급 모델 재시험에서 약 410ms 후 HTTP 500, 파일 도구 호출 0회로 실패했다. 과거 `list_files` 실패는 오류 코드가 저장되지 않아 원인 미확정이며 경로 문제로 단정하지 않는다. 현재 모델의 실제 읽기·쓰기·수정 성공은 미검증이다.
- 빌드 산출물은 갱신했으나 실행 중인 사용자 서버는 재시작하지 않았다. 새 안내와 진단 기록은 서버 재시작 후 적용된다.

## 2026-09-20 작업 폴더 정책 안내 개선

- 화면 표시명을 `고정 폴더` → `지정 폴더와 하위 전체`, `호출자 폴더` → `허용 범위 안에서 호출마다 선택`으로 변경했다. 하위 폴더 포함 여부, 호출별 선택 범위와 형제 폴더 제외, 메타데이터 미지원 시 선택할 정책을 설명한다.
- 시험 입력은 `시험 실행에서 사용할 폴더`로 표시하고 실제 파일에 작용하며 MCP 호출의 기본 폴더가 아님을 안내한다. MCP 연결 탭·실효 도구 안내·README·제품 명세도 동기화했다. 정책 저장값과 권한 로직, 사용자 에이전트 설정은 변경하지 않았다.
- 웹 빌드·typecheck 통과. E2E는 첫 실행의 초안 복원 단계에서 실패했으나 재실행은 1 passed·exit 0이었다. 상세는 TEST_PLAN을 따른다.

## 2026-09-20 NVIDIA 응답 지연 점검

- 실연결 부분 검증: NVIDIA 모델 목록 조회 HTTP 200, 등록된 `moonshotai/kimi-k3`·`z-ai/glm-5.3` 존재 확인. 두 모델의 짧은 응답 시험은 각각 약 121초·122초 후 HTTP 504 `PROVIDER_TIMEOUT`으로 종료됐다.
- MCPex를 우회한 직접 스트리밍 요청(`max_tokens: 128`)도 각 45초 내 응답 헤더를 받지 못했다. 공급업체 내부 대기와 네트워크 경로 중 원인은 미확정이며 실제 생성 성공은 미검증이다. 인증값과 저장 설정은 변경하지 않았다.
- 응답 시험 UI에 대기 표시·중복 제출 방지·이전 결과 초기화·빈 응답 안내를 구현했다. 웹 빌드와 typecheck 통과; 브라우저 검증 결과는 TEST_PLAN.md에 기록한다.

## 2026-09-18 독립 검증 및 실제 MCP 연결 확인

N04·N05 수정 재확인, build/typecheck/lint·68개 자동 테스트·Edge E2E 1개 통과. 등록된 MCPex 도구 5개 모두 실제 호출 성공. 코드 조사·코딩은 package.json을 각각 read_file로 읽고 응답과 실행 이벤트를 대조했다. 파일 변경·명령 실행은 없었다. T14/T15는 이제 부분 검증이며 재연결·동적 목록·취소·실제 쓰기/명령·다른 모델은 미검증이다. 상세 근거는 [검토 보고서](docs/IMPLEMENTATION_REVIEW.md)를 따른다.

## 2026-09-18 N05 허용 commandId 모델 전달 — 수정·검증 완료

- `run_command`의 모델 스키마에 적용된 commandId enum과 label을 제공한다. executable 경로는 모델 요청에 포함하지 않는다.
- 허용 명령이 없으면 `run_command`를 모델 제공 도구, MCP 연결 정보의 실효 도구와 쓰기 가능 annotation에서 제거한다.
- 전체 build/typecheck/lint와 21개 파일·68개 테스트가 통과했다. Edge E2E는 기존 초안 복원 단계에서 1회 실패 후 동일 빌드 재실행이 통과했다. 최신 검토의 N01–N05 코드 수정 항목은 모두 해결됐다.

## 2026-09-18 N04 junction workspace 경계 — 수정·검증 완료

- scoped 작업 폴더의 파일시스템 루트부터 선택 폴더까지 심볼릭 링크와 Windows junction을 검사하고 실제 경로를 확정한다.
- caller 허용 루트 포함 검사, 실행 스냅샷, 큐 잠금과 파일 도구가 같은 실제 경로를 사용한다. 도구 호출 시 root를 다시 검사해 접수 뒤 링크 교체도 차단한다.
- root 자체·중간 상위 junction과 caller API 거부 회귀를 추가했다. 전체 build/typecheck/lint, 21개 파일·68개 테스트와 Edge E2E 1개가 통과했다. 최신 검토에서 남은 코드 항목은 N05다.

## 2026-09-18 수정 후 독립 재검토 — N04·N05 미해결

N01–N03 및 E2E 무인 종료 수정을 재확인했다. 전체 build/typecheck/lint, 21개 파일·67개 자동 테스트와 Edge E2E 1개(exit 0, 수동 개입 없음)가 통과했다. 추가 통합 검사에서 caller 작업 폴더 자체의 junction을 통한 허용 루트 밖 접근(N04), 등록된 commandId/label이 모델 요청에 전달되지 않는 문제(N05)를 확인했다. 상세 재현과 수정 기준은 [검토 보고서](docs/IMPLEMENTATION_REVIEW.md)에 기록했으며 **두 항목은 미해결**이다. 이번 검토는 제품 코드·개인 설정을 수정하지 않았다.

## 2026-09-18 Playwright E2E 무인 종료 지연 — 수정·검증 완료

- Windows에서 `webServer.command`로 실행한 Node 테스트 서버가 Playwright가 종료한 셸의 자식으로 남아 runner 종료를 지연시키던 구조를 제거했다.
- E2E 서버를 Playwright `globalSetup`에서 시작하고 반환한 teardown에서 Fastify와 모의 HTTP 서버를 직접 닫도록 수명주기를 일원화했다.
- `npm run test:e2e`를 연속 2회 실행해 각각 1개 시나리오 통과, exit 0, 포트 47931 listener 정리를 확인했다. 수동 PID 종료는 사용하지 않았다.

## 2026-09-18 N03 workspace 잠금 경계 — 수정·검증 완료

- 경로 포함 판정이 모든 `..` 접두사를 상위 이동으로 오인하던 조건을 고쳐, 정확한 `..` 또는 `..` 뒤 경로 구분자만 부모 이동으로 처리한다.
- 부모 잠금은 일반 자식과 `..cache` 같은 합법적 하위 폴더를 모두 차단하고, 자식 잠금도 부모를 차단한다. 독립 경로는 기존처럼 동시에 실행할 수 있다.
- 전체 build/typecheck/lint와 21개 파일·67개 테스트가 통과했다. 이로써 최신 재검토의 N01–N03 코드 수정 항목은 모두 해결됐다.

## 2026-09-18 N02 전체 접근 도구 스키마 — 수정·검증 완료

- full 실행에서 모델에 제공하는 모든 파일 도구 `path`와 `run_command.cwd`를 필수 절대 경로로 설명하는 정책별 도구 정의를 추가했다. 실행기의 절대 경로 강제와 공개 스키마가 일치한다.
- fixed/caller 실행은 기존 상대 경로 계약을 유지하며 `list_files.path`와 `run_command.cwd`를 생략하면 작업 폴더를 기본값으로 사용한다.
- 정의 단위 검사, 절대 경로 목록·명령 실행, 서버가 실제 모델 요청에 보낸 full 스키마와 해당 스키마에 따른 도구 호출을 검증했다. 전체 build/typecheck/lint와 21개 파일·66개 테스트가 통과했다.

## 2026-09-18 N01 동시 자동 시작 경쟁 — 수정·검증 완료

- 두 클라이언트가 서비스 부재를 동시에 확인해 데몬을 각각 시작하더라도, 데이터 잠금 경쟁에서 진 자식의 exit 오류를 즉시 최종 실패로 확정하지 않는다. 남은 시작 제한 시간 안에 같은 주소의 MCPex health가 준비되면 승리한 서비스를 재사용한다.
- spawn 오류, 명시적 시작 오류와 다른 서비스 응답은 기존처럼 즉시 실패하고, 경쟁 가능 exit 뒤에도 서비스가 준비되지 않으면 원래 시작 오류를 반환한다.
- 동시 호출 회귀 테스트를 추가했다. 전체 build/typecheck/lint와 21개 파일·65개 테스트가 통과했고, 빌드 산출물의 실제 동시 자동 시작도 두 호출 fulfilled·health 200을 확인한 뒤 테스트 프로세스와 임시 데이터를 정리했다.

## 2026-09-18 독립 재검토 — 추가 수정 반영

독립 재검토에서 확인한 동시 자동 시작 실패(N01), 전체 접근 도구 스키마와 필수 경로 불일치(N02), `..cache` 하위 폴더 잠금 누락(N03)을 모두 후속 수정·검증했다. [최신 검토 보고서](docs/IMPLEMENTATION_REVIEW.md)를 따른다. 아래 기능별 구현·자동 검증 완료 기록은 전체 실사용 인수 완료를 의미하지 않는다.

## 2026-09-18 P4 외부 영향 범위 안내 — 구현·자동 검증 완료

- 에이전트 편집 화면의 실효 도구 영역에 파일 도구 결과가 선택한 모델 제공자에게 전달되고, 클라우드 모델이면 파일 내용·경로가 PC 밖으로 전송될 수 있음을 표시한다.
- `run_command`가 실효 도구일 때 허용 명령이 MCPex 프로세스의 현재 OS 사용자 권한으로 실행되며 OS sandbox가 아님을 표시한다.
- 같은 안내를 적용 버전 기준 MCP `tools/list` 설명과 MCP 연결 정보 API에 자동 추가한다. 응답 전용이나 정책상 비활성인 도구에는 권한이 있는 것처럼 표시하지 않는다.
- 전체 build/typecheck/lint와 21개 파일·64개 테스트가 통과했다. Edge E2E에서 UI 파일 전송·명령 권한 안내와 연결 화면의 적용 버전 설명을 확인했으며, 기존 runner 종료 정리 지연으로 성공 출력 후 수동 종료했다.

## 2026-09-18 실사용 개선 UX07 — 구현·자동 검증 완료

- 작업 폴더 정책에 사용자가 명시적으로 선택하는 `전체 접근 (고위험)`을 추가했다. 현재 MCPex OS 사용자 권한 범위이며 권한 상승은 하지 않고, 기존 에이전트 정책은 변경하지 않는다.
- 전체 접근의 파일 경로와 명령 `cwd`는 절대 경로만 허용한다. 선택 도구·명령 allowlist·기대 해시·크기 및 시간 제한은 유지하고, 심볼릭 링크/junction을 차단하며 OS 권한 거부는 `ACCESS_DENIED`로 정규화한다.
- 전체 접근 실행은 모든 scoped 작업과 전역 직렬화해 서로 다른 루트 표현으로 같은 파일 잠금을 우회하지 못하게 했다. 설정 내보내기와 개인 템플릿에서는 환경 의존 정책을 기존 규칙대로 `none`으로 초기화한다.
- 전체 build/typecheck/lint와 21개 파일·64개 테스트가 통과했다. Edge E2E에서 전체 접근 선택·안내·저장·적용과 MCP 연결 화면의 실효 상태를 확인했으며, 기존 runner 종료 정리 지연으로 성공 출력 후 수동 종료했다.
- Windows 다중 드라이브·UNC 공유, 실제 junction과 ACL 권한 거부의 사용자 환경 수동 검증은 남아 있다.

## 2026-09-18 실사용 개선 UX08 — 구현·자동 검증 완료

- 호출자 폴더 정책 옆에 허용 루트 `C:\Work`와 실제 호출 폴더 `C:\Work\ProjectA` 예시를 추가하고, 허용 루트는 권한 경계이며 실제 폴더는 매 호출 값임을 설명한다.
- UI 시험은 요청 본문의 별도 `workspace`, MCP 호출은 `_meta["io.mcpex/workspace"]`를 사용하고 `input.workspace`와 서로 대신할 수 없음을 표시한다. 공식 OpenAI 문서에 이 사용자 정의 메타데이터의 자동 전달 계약이 없으므로 일반 Codex 등록만으로 프로젝트 폴더가 자동 전달된다고 안내하지 않는다. 지원하지 않는 클라이언트의 안전한 대안은 고정 폴더 정책이다.
- 편집 화면에 선택 도구와 실제 모델에 제공되는 **실효 도구**를 분리해 표시한다. Workspace 도구 모드에서 정책이 `none`이거나 허용 도구가 없으면 저장·적용 전에 원인과 해결 방법을 경고한다.
- `GET /api/v1/mcp-connection`의 활성 도구 정보에 적용 버전 기준 runtime/workspace 상태와 실효 내부 도구 목록을 추가했다. 연결 화면은 MCP 등록·활성 상태와 응답 전용, 고정 폴더 사용 가능, 호출자 메타데이터 필요, 내부 도구 0개 상태를 구분한다.
- 전체 build/typecheck/lint와 21개 파일·61개 테스트가 통과했다. Edge E2E에서 `도구 모드 + none` 경고와 활성 MCP 도구의 내부 도구 0개 표시를 확인했으며 기존 runner 종료 정리 지연으로 성공 출력 후 수동 종료했다.
- 실제 Codex가 MCPex 전용 호출자 메타데이터를 보내는 기능은 확인되지 않았으며 자동 전달 기능을 구현한 것으로 기록하지 않는다. 메타데이터를 지원하지 않는 클라이언트는 고정 폴더 또는 사용자가 위험을 이해하고 선택한 전체 접근 정책을 사용할 수 있다.

## 2026-09-18 실사용 개선 UX06 — 구현·자동 검증 완료

- MCP 연결 화면을 실제 Codex 등록 순서인 이름, 유형, 실행 명령, 개별 인자, 환경 변수 키/값, 환경 변수 패스스루, 작업 중인 디렉터리에 맞춰 재구성했다. 필드별 복사 값은 JSON 표현이 아닌 원시 값을 사용한다.
- 공식 Codex `config.toml` 형식의 전체 설정 복사를 추가하고, TOML 경로 이스케이프와 등록 화면용 원시 경로를 구분했다. 패스스루와 `cwd`는 현재 비워 두며 `cwd`와 에이전트 파일 접근 정책이 별개임을 안내한다.
- 로컬 STDIO 연결과 원격 연결을 분리해 원격은 현재 미지원으로 표시하고 loopback 주소·로컬 인증 토큰을 외부 연결에 사용하지 않도록 했다.
- 전체 build/typecheck/lint, 20개 파일·58개 테스트가 통과했다. Edge E2E 기능 시나리오도 통과했으며 기존 runner 종료 정리 지연으로 성공 출력 후 수동 종료했다. Codex CLI가 일회성 설정의 STDIO command/args를 정상 해석하는 것도 확인했다.
- 실제 사용자 Codex 설정에 영구 등록한 뒤 `/mcp` 도구 목록·호출·재연결을 확인하는 T14는 미검증이다. UX07 전체 파일 접근과 UX08 호출자 폴더 설명·유효 도구 안내는 2026-09-18 구현했다.

## 2026-09-16 VBS 자동 시작 잠금 오판 — 수정·검증 완료

- 기본 데이터 폴더의 이전 `service.lock` PID가 Windows에서 `msedgewebview2` 프로세스에 재사용되어, 실행 중인 MCPex가 없는데도 `DATA_DIR_LOCKED`로 오판하고 `open`이 20초 후 실패하는 원인을 확인했다.
- 잠금 파일을 version 1 JSON으로 바꿔 PID, 프로세스 시작 시각, 실행 파일과 소유 토큰을 기록한다. Windows에서 현재 PID의 실제 시작 시각·실행 파일이 기록과 일치할 때만 살아 있는 잠금 소유자로 인정하며 기존 숫자 PID 잠금도 호환한다.
- 잠금 해제는 소유 토큰이 일치하는 파일만 제거해 다른 인스턴스의 잠금을 지우지 않는다. 백그라운드 서비스가 시작 직후 종료되면 20초 polling에 들어가기 전에 종료 코드를 전달한다.
- PID 재사용 모의 잠금 자동 복구, 실제 소유자 중복 차단, 초기화 실패 후 해제를 회귀 테스트로 확인했다. 전체 build/typecheck/lint와 19개 파일·56개 테스트가 통과했다.
- 실제 기본 데이터 폴더의 오래된 잠금을 새 빌드가 자동 복구한 뒤 `node ... open`과 `MCPex Settings.vbs`가 각각 종료 코드 0, `/health`가 정상 응답함을 확인했다. 실제 탐색기 더블클릭 동작은 사용자 확인 전까지 별도다.

## 2026-09-15 실사용 개선 UX05 — 구현·자동 검증 완료

- **MCP 연결 정보** 탭을 추가해 MCPex 서버를 클라이언트마다 한 번 등록하고 활성 에이전트가 개별 도구로 노출되는 구조를 안내한다.
- CLI가 현재 Node 실행 파일, CLI 절대 경로와 `mcp` 인수, 필요한 환경변수를 서버에 전달한다. 화면은 실행 파일·인수·환경변수와 일반 JSON 예시를 분리해 표시하고 복사 동작을 제공하며 인증 토큰·provider credential은 포함하지 않는다.
- `GET /api/v1/mcp-connection`은 적용 버전을 기준으로 활성 도구 이름·설명을 계산하고 비활성·미적용 에이전트를 사유별로 구분한다. 관리 API 정상 응답과 실제 MCP 클라이언트 미확인 상태를 별도로 표시한다.
- 등록→에이전트 저장·적용·활성화→도구 목록 확인→필요 시 재연결 순서, 활성 도구 0개와 CLI 밖 직접 서버 실행 시의 다음 조치를 제공한다.
- 인증 요구, 공백 포함 경로와 분리 인수, 적용/초안 설명 격리, 비밀 제외를 API 테스트로 확인했다. Edge E2E에서 연결 탭·상태·실행 경로·인수·활성 도구 2개·비밀 제외 안내를 통과했다. 실제 사용자 MCP 클라이언트 등록·호출은 T14에 남아 있다.
- build/typecheck/lint와 `npx vitest run --maxWorkers=2` 19개 파일·55개 테스트가 통과했다. Playwright 1개 시나리오도 통과했고 기존 종료 정리 지연 때문에 테스트 전용 서버를 식별해 종료한 뒤 runner exit 0을 확인했다.

## 2026-09-15 실사용 개선 UX01 — 구현·자동 검증 완료

- 프로바이더·모델 목록과 에이전트 편집의 일관된 위치에 삭제 동작을 추가하고, 대상 이름과 영향을 확인한 뒤 실행하도록 했다. 삭제 확인을 취소하면 데이터를 변경하지 않는다.
- 참조 중인 프로바이더·모델은 서버의 기존 차단을 유지하면서 모델 또는 에이전트를 먼저 정리하도록 안내한다. 활성 에이전트의 삭제 버튼은 비활성화하고 비활성화가 선행되어야 함을 표시한다.
- 미저장 편집 취소, 적용 전 에이전트 전체 삭제, 적용된 에이전트의 저장 초안 복원을 구분했다. `POST /api/v1/agents/:id/draft-discard`는 정확한 적용 버전 스냅샷과 `expectedRevision`으로 초안만 복원하며 공개 버전·활성 상태·실행 기록은 유지한다.
- 본문 없는 DELETE 요청에는 JSON Content-Type을 보내지 않도록 공통 API 호출을 바로잡았고, 삭제 후 목록과 선택 상태를 다시 동기화한다.
- API 회귀 테스트와 Edge E2E에서 초안 복원 revision 충돌 및 상태 보존, 삭제 확인 취소/확정, 참조 충돌 안내, 모델→프로바이더 삭제 순서, 활성 삭제 제한, 미저장 편집 취소를 통과했다. 실제 사용자 데이터와 키보드·스크린리더 수동 검증은 남아 있다.
- build/typecheck/lint와 `npx vitest run --maxWorkers=2` 18개 파일·54개 테스트가 통과했다. Playwright 기능 시나리오는 통과했지만 기존 runner 종료 정리 지연으로 프로세스를 수동 종료했다.

## 2026-09-15 실사용 개선 UX03 — 구현·자동 검증 완료

- 시험 실행 접수 즉시 run ID와 대기·실행·취소 요청 상태를 같은 편집 영역에 표시하고 실행 상세 API를 terminal 상태까지 자동 조회한다.
- 완료 답변은 읽기 쉬운 결과 영역에, JSON·부분 결과와 오류는 별도 영역에 표시하며 전체 실행 객체는 펼침 상세로 제공한다. 진행 중 취소와 조회 연결 실패 후 다시 조회를 지원한다.
- 화면 또는 에이전트 전환 시 기존 조회를 중단하고 요청 세대로 연속 시험을 격리해 늦은 이전 결과가 현재 결과를 덮어쓰지 않는다. 서버의 failed/cancelled/timed_out/interrupted 상태를 서로 다른 종료 안내로 표시한다.
- 추적 단위 테스트 3개와 Edge E2E에서 느린 실행 진행 상태, 연속 실행 결과 격리, 최종 답변 자동 표시, 원시 상세, 조회 실패 후 재조회를 통과했다. 실제 장시간 모델과 모든 오류 상태의 브라우저 수동 검증은 남아 있다.
- build/typecheck/lint와 `npx vitest run --maxWorkers=2` 18개 파일·53개 테스트가 통과했다. Playwright 기능 시나리오는 통과했지만 기존 runner 종료 정리 지연으로 프로세스를 수동 종료했다.

## 2026-09-15 실사용 개선 UX02 — 구현·자동 검증 완료

- 에이전트의 현재 `inputSchema`에서 문자열·숫자·정수·boolean·enum 시험 입력 폼을 생성하고 필수 여부·설명·기본값을 표시한다. 일반 문장은 JSON을 직접 작성하지 않고 올바른 객체로 조립한다.
- 중첩 객체·배열·null은 고급 JSON 대상임을 명시하고 전체 스키마 예제를 생성한다. 폼/JSON 전환과 스키마 변경 시 호환 값은 유지하고 제거되거나 타입이 달라진 값은 예제로 정리한다.
- JSON 구문, 최상위 객체, 필수값, 타입·enum·길이·숫자 범위·배열 크기 오류를 구분해 입력 영역과 해당 최상위 필드에 표시한다. `input.workspace`와 실행 권한용 시험 호출 작업 폴더도 구분해 안내한다.
- 변환·검증 단위 테스트 4개와 Edge E2E에서 일반 응답, 사용자 정의 문자열·정수·boolean·enum 스키마, 코드 구현 템플릿의 다중 필수 입력과 미리보기를 통과했다. 실제 사용자 스키마 전체 조합과 스크린리더 수동 검증은 남아 있다.
- build/typecheck/lint와 `npx vitest run --maxWorkers=2` 17개 파일·50개 테스트가 통과했다. Playwright 기능 시나리오는 통과했지만 기존 runner 종료 정리 지연으로 프로세스를 수동 종료했다.

## 2026-09-15 실사용 개선 UX04 — 구현·모의 브라우저 검증 완료

- 성공·오류 안내를 현재 스크롤과 무관한 고정 알림 영역으로 옮겼다. 성공 안내는 `status`/polite, 오류는 `alert`/assertive로 알리고 두 알림 모두 편집 포커스를 이동하지 않는 닫기 동작을 제공한다.
- 오류는 새 작업 시작이나 후속 성공 안내로 자동 제거되지 않으며, API가 제공한 details가 있을 때 펼쳐 확인할 수 있다. 초기 연결 실패도 조치 가능한 오류 알림으로 통일했다.
- Edge E2E에서 긴 편집 화면 하단의 알림 viewport 노출, 409 오류가 후속 성공 안내 중 유지되는 동작, 실패 입력 보존과 닫기를 확인했다. build/typecheck/lint와 `npx vitest run --maxWorkers=2` 16개 파일·46개 테스트가 통과했다.
- 기존 Playwright runner 종료 정리 지연은 재현되어 기능 시나리오 통과 후 테스트 프로세스를 수동 종료했다. 실제 사용자 환경의 좁은 창·키보드·스크린리더 수동 검증은 남아 있다.

## 2026-09-15 설정 실행 파일·서비스 자동 시작

- 구현 완료: `MCPex Settings.vbs` 더블클릭으로 open 실행. open/mcp는 로컬 서비스가 없으면 숨김 백그라운드 서비스 시작 후 준비를 기다리고, 실행 중이면 재사용한다. 사용자 데이터·활성 상태는 변경하지 않는다.
- 검증 완료: 임시 데이터·별도 포트에서 실제 Node 서비스 동시 자동 시작 요청, 인증 API 200, 기존 PID 재사용 확인. 테스트 프로세스와 임시 데이터를 정리했다.
- 검증 완료: 비활성화 후 서비스 재시작 시 enabled=false 유지 및 MCP 목록 제외, 빈 목록으로 연결한 뒤 도구 활성화 시 재연결 없이 목록 조회.
- build/typecheck/lint 통과. 전체 기본 병렬 테스트에서 기존 P0 인증 테스트 1회 5초 timeout 발생; `npx vitest run --maxWorkers=2`로 재실행하여 16개 파일·46개 테스트 통과. 기본 병렬 실행의 부하 민감성은 남아 있다.
- 미검증: 실제 Windows 탐색기에서 VBS 더블클릭→브라우저 자동 열기, 실제 사용자 MCP 앱 자동 시작. 기존 사용자 서비스를 재시작하거나 외부 MCP 설정을 변경하지 않았다. 새 CLI/서버는 빌드 완료됐으며 이미 떠 있는 서버는 기존 프로세스 코드를 유지한다.

## 2026-09-15 실사용 인증 문제 — 수정·검증 완료

실행 중 서버의 메모리 토큰과 현재 DPAPI 토큰이 달랐고 서버가 이 변경을 감지하지 않아 `open`의 bootstrap 요청이 HTTP 401로 실패했다. 암호화된 토큰 revision이 달라질 때만 DPAPI 값을 다시 읽어 캐시를 갱신하도록 수정했다. 토큰 교체 회귀 테스트와 실제 기본 데이터의 bootstrap→세션 교환→관리 API가 모두 통과했다. 토큰을 교체한 외부 원인은 확정하지 않았으며 상세 증거와 남은 검증은 [추가 검증 항목](docs/VERIFICATION_GAPS.md)에 기록했다.

## 2026-09-15 V01~V04 수정 후 재확인

독립 검토에서 V01~V04 수정 반영과 관련 회귀 테스트 통과를 확인했다. 새 빌드 기준 자동 테스트 40개, build/typecheck/lint 통과. Edge E2E 기능 시나리오 1개도 통과했으나 runner 종료를 위해 이번 실행의 테스트 서버를 수동 정리했다. 무인 E2E 종료 및 기존 실환경 미검증 항목은 완료 처리하지 않는다. 제품 소스는 이번 검토에서 수정하지 않았다.

## 2026-09-15 독립 재검토 보충

R01~~R12 개선의 실제 코드 반영과 40개 자동 테스트·빌드·타입 검사·lint 통과를 확인했다. 후속 V01 STDIO workspace 전달, V02 취소·deadline telemetry 보존, V03 provider 시험·조회 timeout, V04 resource group 재설정을 모두 보완했다. 이번 재검토에서 확인한 코드 연결 문제는 남아 있지 않다. E2E 재실행은 기존 테스트 포트 점유로 시작하지 못했으며 기존 통과 결과는 역사적 기록으로 유지한다.

기준일: 2026-09-15.

## 전체 검토에 따른 상태 보정

2026-09-13 전체 검토에서 P0~P4의 일부 필수 기능이 미구현 또는 연결되지 않은 것을 확인했다. 아래 기존 단계별 완료 표시는 각 구성 요소의 이전 보고이며, 제품 단계 전체 완료를 뜻하지 않는다. 현재 종합 상태는 **부분 구현, v1 인수 미완료**다. 인증 적용, 실행 도구 정책, UI 연결·에이전트 편집, 입력·출력 검증, 취소·종료 처리 등을 먼저 보완해야 한다. 상세 근거는 [전체 구현 검토](docs/IMPLEMENTATION_REVIEW.md)에 기록했다.

## 전체 검토 후 보완 작업

- **R01 검증 완료:** DPAPI 보호 로컬 접속 토큰, bootstrap/MCP Bearer 인증, 만료되는 UI 세션, 전체 관리 API 인증, localhost Host 허용 목록, 세션 쓰기 요청 Origin·CSRF 검사를 적용했다.
- **R02 검증 완료:** dispatcher 실행 직전 활성 도구 허용 목록과 도구별 인자를 검사하고, tools 실행 모드 및 MCP annotation을 실제 실행 권한과 일치시켰다.
- **R03 구현 완료·부분 검증:** Vite proxy, 서버 정적 UI 제공, CLI bin 경로, `mcpex open`의 bootstrap·fragment 브라우저 접속을 연결했다. 정적 제공 테스트와 로컬 브라우저 렌더링을 확인했다.
- **R04 구현 완료·부분 검증:** 다중 프로바이더/모델 선택, 템플릿 기반 에이전트 생성, 핵심 에이전트 초안 편집·시험·적용·활성화, 실행 목록 API/UI를 연결하고 산출물 없는 web 전용 TypeScript 검사를 추가했다.
- **R05 검증 완료:** 제한된 JSON Schema 2020-12를 적용·호출·출력 단계에 연결하고 MCP HTTP/STDIO 도구 정의에 실제 입력 스키마를 전달한다. JSON 출력과 스키마 불일치, 비JSON, 128KiB 초과를 실패 처리하고 상한 내 원문과 구체 오류 코드를 실행 기록에 보존한다. UI에도 JSON 출력 스키마 편집을 추가했다.
- **R06 구현 완료·부분 검증:** 접수 시 절대 deadline, provider 요청 timeout, UI 취소 API와 MCP caller signal을 모델 fetch·도구까지 전파하고 `cancelled`/`timed_out` 상태를 구분했다. 모의 무응답 provider와 큐 대기·실행 취소는 통과했으며 실제 MCP 클라이언트 취소는 미검증이다.
- **R07 구현 완료·부분 검증:** SIGINT/SIGTERM graceful shutdown, 진행 실행 정리, stale lock 회복·live lock 거부, 초기화 실패 정리와 재시작 interrupted 전환을 구현했다. 저장소 단위·서버 재시작 검증은 통과했으며 실제 CLI 프로세스 signal 검증은 미수행이다.
- **R08 검증 완료:** 허용 명령 설정의 구조·고유 ID·절대 executable 경로를 검증해 실제 `WorkspaceTools`에 전달하고 UI 편집을 연결했다. 서버 tool loop의 allowlisted Node 명령 실행과 상대 executable 적용 거부를 통과했다.
- **R09 검증 완료:** provider와 resource group 동시성 제한을 큐에 연결하고 모델 기본 생성값과 agent override를 병합해 preview·adapter 요청에 적용한다. 동일 provider의 서로 다른 모델 직렬화와 공유 resource group 제한을 통과했다.
- **R10 검증 완료:** STDIO 브리지가 백엔드 tool 목록을 캐시 우회 재조회하고 추가·변경·삭제를 등록 상태에 반영해 상위 클라이언트에 목록 변경을 알린다. 인메모리 MCP 왕복은 통과했으며 실제 STDIO 자식 프로세스 검증은 미수행이다.
- **R11 검증 완료:** DB schema v2에서 run별 해석 완료 설정 스냅샷을 저장하고, 적용 버전에는 에이전트·모델·프로바이더 설정을 함께 고정한다. 인증값은 접수 시 메모리에만 고정한다. UI 시험 실행은 즉시 `202 queued`를 반환하고 상태 API에서 terminal 결과를 조회한다. v1→v2 마이그레이션, 비동기 접수, run 스냅샷, 적용 후 초안과 MCP 설명 분리를 통과했다.
- **R12 검증 완료:** 파일 읽기·교체·기존 파일 hash 확인에 제한 읽기를 공통 적용하고 검색에서 초과 파일을 전체 로드하지 않는다. 파일 목록·검색 결과는 직렬화 크기 예산으로 자르며 명령 stdout/stderr는 실행 중 합산 상한까지만 보관한다. 초과 여부와 관측한 원래 바이트를 observation에 기록한다.
- **P6 프로바이더·모델 관리 검증 완료:** 상세 조회와 expectedRevision 수정, 프로바이더 인증값 제거, 미참조 항목 삭제를 공개 API에 연결했다. 등록 모델이 있는 프로바이더와 현재 초안·적용 버전에서 참조하는 모델의 삭제를 409로 거부한다.
- **P6 템플릿·에이전트 관리 검증 완료:** 기본 템플릿 5종을 누락분만 초기화하고 개인 템플릿 상세·생성·수정·삭제, 선택 설정 묶음 차이와 초안 적용을 구현했다. 개인 템플릿은 모델·workspace·명령 허용을 제거한다. 에이전트 복제와 비활성 soft delete, 적용 후 toolName 변경 금지·삭제 이름 재사용 금지를 API와 UI에 연결했다.
- **추가 실행 안전성 검증 완료:** workspace 잠금 대기 작업을 실행 슬롯 집계 전에 건너뛰어 독립 작업이 진행되게 했다. caller workspace 정책을 UI와 MCP 호출 메타데이터에 연결하고, 누락·허용 루트 이탈 거부와 검증된 실행 경로의 스냅샷 저장을 확인했다.
- **V01 STDIO caller workspace 검증 완료:** 브리지가 `_meta["io.mcpex/workspace"]` 문자열만 백엔드 MCP 호출에 전달하고 다른 임의 메타데이터는 제거한다. 공식 SDK 인메모리 client를 사용해 실제 ToolCatalogBridge 전달 경계를 확인했다.
- **V02 취소 telemetry 보존 검증 완료:** RunQueue가 취소·deadline 오류를 변환할 때 원래 오류를 cause로 보존하고 서버가 cause chain에서 telemetry를 회수한다. read→write 후 timeout된 MCP 실패 envelope에 도구 호출·변경 경로와 완료된 모델 usage가 남는 것을 확인했다.
- **V03 provider 관리 요청 timeout 검증 완료:** 모델 목록 조회와 probe가 provider의 `requestTimeoutMs` 및 클라이언트 연결 종료 signal을 adapter fetch에 전달한다. 무응답 provider에서 두 API가 HTTP 504 `PROVIDER_TIMEOUT`으로 종료됨을 확인했다.
- **V04 resource group 재설정 검증 완료:** 큐의 그룹 제한 맵을 현재 provider 전체의 그룹별 최솟값으로 교체한다. 제한 상향, 그룹 이동, 낮은 제한 provider 삭제 후 동시 실행 한도가 즉시 다시 늘어남을 실제 관리 API와 실행 큐로 확인했다.
- **명령 환경 격리 검증 완료:** `run_command`와 Windows 프로세스 종료 헬퍼가 부모 환경 전체를 상속하지 않고 플랫폼별 최소 OS 변수만 전달한다. 임의 비밀 환경변수와 `NODE_OPTIONS` 차단을 자식 프로세스에서 확인했다.
- **파일 충돌 계약 검증 완료:** 새 파일은 해시 없이 생성할 수 있지만 기존 파일의 전체 쓰기와 텍스트 교체는 직전 `read_file`의 `expectedHash`를 필수로 요구한다. 해시 누락·불일치 거부와 파일 내용 보존, 최신 해시 수정 성공을 확인했다.
- **MCP 실행 결과 계약 검증 완료:** 성공과 애플리케이션 실패 envelope를 `structuredContent`와 JSON 텍스트에 함께 제공한다. 실제 도구 호출 수·변경 경로·명령 결과·잘림, 모든 모델 턴의 usage 합계, 출력 검증 상태와 접수부터 완료까지 duration을 반환한다.
- **P6 실행 이벤트·SSE 검증 완료:** DB schema v3의 `run_events`에 queued/started/model/tool/cancel_requested/finished 이벤트를 실행별 단조 `seq`로 저장한다. SSE는 저장 이벤트를 재생한 뒤 terminal까지 이어 보내며 `afterSeq`와 `Last-Event-ID`로 재개한다. 모델 사용량과 provider request ID를 비밀정보 없이 `model.finished`에 기록한다.
- **P6 설정 이동·보존·백업 검증 완료:** 설정 파일에서 인증값·민감 헤더/필드·절대 작업 폴더·명령 경로를 제거하고, 가져오기 충돌을 미리 본 뒤 새 UUID의 비활성 초안으로 단일 트랜잭션 적용한다. DB schema v4에서 1~365일 보존 설정과 만료 본문·이벤트 정리, SSE 410을 구현했다. migration 전 백업과 사용자 요청 온라인 SQLite 백업을 생성한다.
- **P6 오류 UI·수직 E2E 검증 완료:** API 오류의 HTTP 상태·code·message·details와 상태별 해결 안내를 접근 가능한 alert로 표시하며 실패한 폼 입력을 유지한다. Playwright와 Microsoft Edge로 fragment 인증·제거, provider/model 등록, 서로 다른 템플릿의 agent 2개, 충돌 오류, 시험 실행, 적용·활성화, 실행 기록, 편집 재적용을 통과했다.
- **자동 검증:** 2026-09-15 `npm test` 15개 파일·40개 테스트와 build, typecheck, lint 통과. 이전 `npm run test:e2e` 1개 테스트 통과 이력은 있으나 최신 재실행은 포트 점유로 시작하지 못했다. 설정 이동 화면의 파일 선택 조작과 백업 파일로 서비스를 재기동하는 전체 복구는 미검증이다.

## 설계 및 문서

- **완료:** 제품 범위, 프로바이더/모델/에이전트 분리, 템플릿 편집, MCP 노출 방식 결정.
- **완료:** 기술 스택, 프로세스·모듈 경계, 데이터·API·실행 계약 및 단계별 인수 기준 문서화.
- **문서 검증 완료:** 상대 링크 20개 누락 없음, 새 설계 문서 5개 등록 확인, 새 문서 공백 검사 통과. 앱 검증은 해당하지 않음.
- **P0 구현 완료:** npm workspace, 공유 계약, Node 내장 SQLite 저장소, 데이터 디렉터리 잠금, Windows CurrentUser DPAPI SecretStore, Fastify health/bootstrap/exchange API, CLI serve/open 골격, React 빈 상태 UI, 품질 명령과 P0 테스트를 추가했다.
- **P1 구현 완료:** provider/model 저장소와 CRUD API, DPAPI credential 참조 저장, OpenAI Chat Completions adapter, 모델 목록 조회·응답 probe API, provider URL credential 차단, 프로바이더·모델·응답 시험 UI를 추가했다.
- **P2 구현 완료:** agent/template/run 저장소, 에이전트 초안 CRUD와 expectedRevision 충돌, 불변 적용 버전, 기본 템플릿, 입력 치환 preview, 응답 모드 시험 실행과 실행 기록 조회를 추가했다.
- **P3 구현 완료:** 공식 MCP SDK v2 기반 Streamable HTTP `/mcp`, 활성 에이전트 동적 tool 등록, 비활성 에이전트 차단, MCP 실행 envelope·run 기록, `mcpex mcp` STDIO bridge를 추가했다.
- **P4 구현 완료(부분 검증):** `@mcpex/tools`에 workspace 경로 방어, list/read/search/write/replace 파일 도구, hash 충돌 검사, 출력·크기 한도, shell=false 명령 allowlist 실행기를 추가했다. `@mcpex/providers`의 표준 tool-call 파싱, `@mcpex/runtime`의 동시성 큐·겹치는 workspace 직렬화·반복 실행기를 서버 agent 실행 경로와 연결했다.
- **P5 진행 중:** Anthropic Messages, Gemini `generateContent`, Vertex AI Gemini, Amazon Bedrock Converse adapter와 공통 응답·tool-call 변환, provider-manager 기준의 provider profile registry, 대표 클라우드·로컬 프로필, NovelAI OpenAI 호환 프로필, adapter 선택 UI와 모의 계약 테스트를 추가했다. 실제 연결·취소·오류 경계 검증은 남아 있다.
- 기존 gemma-agent와 참고 플러그인은 별도 자료이며 MCPex에 통합되지 않았다.

## 기능 상태

- 2026-09-16 추가 요구 [UX06–UX08](docs/USABILITY_ISSUES.md)은 구현·자동 검증을 완료했다. 실제 사용자 Codex 등록·도구 호출과 UX07의 Windows 다중 드라이브·UNC·ACL 경계는 미검증이다. README는 현재 기능의 영역별 사용법으로 재작성했으며 실제 등록 검증이 끝나지 않은 MCP 절차는 아직 제외한다.

- 실사용 개선 UX01–UX05의 화면과 API 구현 및 자동·모의 브라우저 검증을 완료했다. 실제 사용자 MCP 클라이언트·외부 모델·보조기기와 운영 경계 검증은 별도 미수행 범위다.

2026-09-15 실사용에서 삭제 진입점 부족, 설정별 시험 입력 미지원, 시험 최종 결과 자동 표시 누락, 상단 알림 가시성 문제와 MCP 등록 안내 필요를 접수했다. UX01–UX05는 구현·자동 검증을 완료했다. [실사용 개선 항목](docs/USABILITY_ISSUES.md)에서 상세 상태를 관리한다. 아래 기존 API·모의 검증 완료 표시는 실제 사용자 환경의 수동 검증 완료를 의미하지 않는다.

| 단계                              | 상태                          | 실행 검증                                                                                                                                                     |
| --------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0 기반·저장·인증                 | 구현 완료(부분 검증)          | 빌드·typecheck·lint·format·P0 테스트·DB 재시작·DPAPI 왕복·평문 미기록 통과; 다른 Windows 계정 복호화 실패와 clean install은 미수행                            |
| P1 프로바이더·모델                | 구현 완료(모의 검증 완료)     | 모의 OpenAI 서버에서 등록→목록 조회→모델 등록→동일 프롬프트 응답 시험 통과; 실제 선택 모델·취소·인증 오류는 미검증                                            |
| P2 에이전트·템플릿·응답           | 구현 완료(모의 검증 완료)     | 초안 생성→미리보기→적용 버전 생성→시험 실행→run 조회와 기본 템플릿 조회를 모의 서버에서 통과; JSON Schema 상세 검증·취소·큐·실제 모델은 미검증                |
| P3 MCP 제공                       | 구현 완료(모의 SDK 검증 완료) | 공식 SDK client로 tools/list·tools/call 통과; 활성화되지 않은 에이전트는 목록에 노출하지 않음; 실제 사용자 클라이언트·취소 전파·재연결은 미검증               |
| P4 자체 도구 실행                 | 구현 완료(부분 검증)          | 파일·명령 도구, provider tool-call 파싱, 모델 반복 실행, 서버 test-run 연결, 큐·workspace 직렬화 모의 검증 통과; 취소·재시작 복구·junction·실제 모델은 미검증 |
| P5 프로바이더 확장                | 진행 중                       | 5개 protocol/native adapter와 provider profile registry, 대표 OpenAI 호환 클라우드·로컬 프로필의 모의 검증 통과; 실제 API·취소·오류 경계는 미검증             |
| P6 v1 통합·운영                   | 구현 완료(부분 검증)          | 관리 CRUD, 템플릿·agent 수명주기, 실행 이벤트/SSE, 설정 이동·보존·백업, 오류 UI와 모의 브라우저 수직 E2E 통과; clean install·실환경 복구는 미검증             |
| 출력 검증·외부 실행기·원격 서비스 | 보류                          | 해당 없음                                                                                                                                                     |

## 확인한 환경과 한계

- 설정 실행기 무반응 보완: Windows 기본 브라우저를 PowerShell `Start-Process`로 호출하고 실행기 종료 상태를 확인하도록 변경했다. 2026-09-16에는 Windows PID 재사용으로 오래된 잠금을 실제 다른 프로세스의 살아 있는 잠금으로 오판한 후속 장애를 수정했다. CLI 빌드·정적 검사, 현재 계정의 VBS 실행 종료 코드 0과 health 정상 응답을 확인했으며 실제 탐색기 더블클릭은 사용자 확인 전까지 미검증이다.

- 로컬 Node.js v24.19.0, npm 11.17.0 확인.
- better-sqlite3는 Node 24.19.0/현재 Windows 환경에서 사전 빌드와 Visual Studio C++ 도구가 없어 설치 실패했다. P0 저장소는 Node 24 내장 `node:sqlite`로 구현했으며 운영 안정성은 추가 검증이 필요하다.
- Windows DPAPI 왕복은 현재 계정에서 확인했다. 다른 Windows 계정 실패 경로, 클라이언트 목록 갱신, 실제 모델 도구 호출은 구현 단계 검증 필요.
- P1/P5 adapter는 `openai-chat`, `anthropic-messages`, `gemini-generate-content`, `vertex-gemini`, `bedrock-converse`를 구현했고 NovelAI는 OpenAI 호환 profile로 지원한다. 실제 외부 API 연결·rate limit·timeout·취소와 Gemini 최신 Interactions API 전환은 미검증이며, 실제 API 키를 사용한 검증은 수행하지 않았다.
- P2 시험 실행은 비동기 접수 후 run 상태를 조회하는 방식이다. 큐·취소·도구 반복·JSON 출력 검증은 P3/P4 실행 경로와 통합되어 있다.
- P3는 공식 `@modelcontextprotocol/server`, `@modelcontextprotocol/client`, `@modelcontextprotocol/node` v2 계열을 사용한다. MCP 취소·재연결과 실제 클라이언트 호환은 후속 검증 대상이다.
- 추가 검증 항목은 [docs/VERIFICATION_GAPS.md](docs/VERIFICATION_GAPS.md)에 별도로 관리한다.
- 네이티브 명령 실행은 OS sandbox가 없다는 설계 제한을 문서화했다.

전체 구현 검토의 R01–R12와 추가 정적 검토에서 확인한 코드 연결 누락을 보완했다. P6 관리·이벤트·설정 이동·보존·백업·오류 UI와 모의 수직 E2E도 마쳤다. 남은 작업은 설정 이동 UI 파일 선택·전체 백업 복구, 실제 MCP 클라이언트·모델·프로세스 종료·파일시스템 경계 등 실환경 검증이다.
