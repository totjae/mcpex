# AGENTS.md

이 파일은 프로젝트의 상세 명세를 직접 담는 문서가 아니다.

개발 에이전트가 프로젝트를 이해하고 작업할 때 **어떤 문서를 확인하고, 어떤 정보를 어디에 기록하며, 구현 결과와 문서를 어떻게 동기화할지 안내하는 기준 문서​**로 사용한다.

에이전트의 구현 방식이나 판단을 과도하게 제한하지 않는다.  
프로젝트의 실제 코드, 기존 구조, 관련 문서와 현재 상태를 확인한 뒤 필요한 방법을 스스로 판단하여 작업한다.

---

## 1. 기본 원칙

1. 개발을 시작하기 전에 작업과 관련된 기존 코드와 문서를 확인한다.
2. 프로젝트의 현재 구현 상태와 기존 설계를 우선 파악한 뒤 작업한다.
3. 기능 개발 과정에서 지속적으로 관리할 필요가 있는 정보가 생기면 관련 문서를 작성하거나 갱신한다.
4. 구현 결과가 기존 계획이나 문서와 달라진 경우 실제 구현에 맞게 관련 문서를 갱신한다.
5. 기존 문서에 기록하기 적절하지 않은 새로운 영역이 생기면 별도의 문서를 생성할 수 있다.
6. 신규 문서를 생성한 경우 이 파일의 **현재 문서 목록**에 등록한다.
7. 계획된 내용, 구현된 내용, 검증된 내용과 미검증 내용을 구분해서 기록한다.
8. 아직 결정되지 않은 사항을 임의로 확정된 내용처럼 문서화하지 않는다.
9. 실제 비밀번호, API key, token, private key, secret, DB 접속 정보 등 민감정보를 문서에 기록하지 않는다.

---

## 2. 현재 문서 목록

프로젝트에서 지속적으로 관리하는 주요 문서를 등록한다.

| 문서                  | 위치                                                       | 작성·관리할 내용                                   |
| --------------------- | ---------------------------------------------------------- | -------------------------------------------------- |
| 프로젝트 개요         | [README.md](README.md)                                     | 프로젝트 목적, 실행 방법, 기본 구조                |
| 구현 상태             | [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md)       | 구현 완료, 진행 중, 미구현 및 미검증 상태          |
| 테스트 및 검증        | [TEST_PLAN.md](TEST_PLAN.md)                               | 테스트 방법, 검증 기준과 결과                      |
| 제품 및 화면 명세     | [docs/PRODUCT_SPEC.md](docs/PRODUCT_SPEC.md)               | 확정 제품 범위, 사용자 흐름, 템플릿과 편집 규칙    |
| 아키텍처 및 기술 스택 | [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)               | 기술 선택, 프로세스·모듈 구조, 실행 정책, 운영     |
| 데이터 및 설정 명세   | [docs/DATA_MODEL.md](docs/DATA_MODEL.md)                   | 저장 구조, 설정 필드, 버전, 가져오기·내보내기      |
| API 및 실행 계약      | [docs/API_SPEC.md](docs/API_SPEC.md)                       | 관리 API, MCP 입출력, 큐·실행·취소·오류 계약       |
| 구현 계획             | [docs/IMPLEMENTATION_PLAN.md](docs/IMPLEMENTATION_PLAN.md) | 단계별 구현 순서, 작업 범위, 인수 조건과 후속 항목 |
| 추가 검증 항목        | [docs/VERIFICATION_GAPS.md](docs/VERIFICATION_GAPS.md)     | 구현 후 남은 운영·보안·외부 연동 검증과 보류 판단  |
| 전체 구현 검토 | [docs/IMPLEMENTATION_REVIEW.md](docs/IMPLEMENTATION_REVIEW.md) | 전체 코드 검토 결과, 연결 누락, 우선순위와 수행 검사 |
| 실사용 개선 항목 | [docs/USABILITY_ISSUES.md](docs/USABILITY_ISSUES.md) | 사용자 보고, 코드 대조, UI 개선 작업과 인수 기준 |
| 대상 파일 지정 설계 | [docs/TARGET_FILE_DESIGN.md](docs/TARGET_FILE_DESIGN.md) | 실행별 targets 검증, 대상 ID 기반 도구 연결, 권한·호환성·인수 기준 |
| 런타임 정적 검수 | [docs/RUNTIME_AUDIT.md](docs/RUNTIME_AUDIT.md) | 메모리·비동기·CPU·리소스 수명 위험, 수정 제안과 미수행 검증 |
| 런타임 개선 작업 인계 | [docs/RUNTIME_REMEDIATION_PLAN.md](docs/RUNTIME_REMEDIATION_PLAN.md) | RTA-01–09 수정 순서, 구현 범위, 회귀·실환경 검증과 완료 기준 |

프로젝트에 존재하지 않는 문서는 필요할 때 생성한다.

프로젝트 성격에 따라 다음과 같은 문서를 추가할 수 있다.

- 시스템 구조 및 아키텍처
- API 명세
- 데이터베이스 구조
- UI/UX 명세
- 인증·권한·보안
- 외부 서비스 연동
- 환경 설정
- 배포 및 운영
- 에이전트 또는 모델 구성
- 작업 계획
- 알려진 문제 및 기술 부채

필요하지 않은 문서를 형식적으로 생성할 필요는 없다.

---

## 3. 문서 생성 기준

새로운 문서는 다음과 같은 경우 생성할 수 있다.

- 기존 문서에 포함하기에는 독립적인 기능이나 영역이 생긴 경우
- 반복해서 참고해야 하는 설계나 운영 정보가 생긴 경우
- API, 데이터 구조, 외부 연동 등 명확한 계약이나 명세가 필요한 경우
- 구현 상태와 별도로 장기간 유지해야 하는 기술적 판단이 있는 경우
- 향후 작업자가 다시 조사하지 않아도 되도록 남겨둘 가치가 있는 정보가 있는 경우

일시적인 작업 메모나 쉽게 코드에서 확인 가능한 내용을 별도 문서로 만들 필요는 없다.

---

## 4. 문서 갱신 기준

기능을 변경할 때 모든 문서를 기계적으로 수정할 필요는 없다.

변경으로 인해 기존 문서의 내용이 더 이상 실제 상태와 일치하지 않거나, 향후 작업에 필요한 새로운 정보가 생긴 경우 관련 문서를 갱신한다.

예시:

| 변경 내용                 | 주로 확인할 문서         |
| ------------------------- | ------------------------ |
| 주요 기능 추가·변경       | 기능 명세, 구현 상태     |
| API 추가·변경             | API 관련 문서            |
| DB 구조 변경              | DB 관련 문서             |
| 설정값 또는 환경변수 변경 | 설정·운영 문서           |
| 인증·권한 변경            | 보안·인증 문서           |
| 외부 서비스 연동 변경     | 연동 관련 문서           |
| 배포 방식 변경            | 배포·운영 문서           |
| 테스트 또는 검증 수행     | TEST_PLAN.md             |
| 구현 완료 또는 상태 변경  | IMPLEMENTATION_STATUS.md |
| 주요 구조 변경            | 아키텍처 관련 문서       |

위 표는 참고 기준이며 실제 프로젝트 구조에 따라 판단한다.

---

## 5. 신규 문서 등록 형식

새로운 지속 관리 문서를 생성하면 **현재 문서 목록**에 등록한다.

```markdown
| 문서 표시 이름 | [FILE_NAME.md](FILE_NAME.md) | 이 문서에서 작성·관리하는 내용 |
```

하위 디렉터리에 생성한 경우 실제 상대 경로를 사용한다.

예:

```markdown
| API 명세 | [docs/API_SPEC.md](docs/API_SPEC.md) | API 엔드포인트, 요청·응답 구조와 오류 정의 |
```

---

## 6. 신규 문서 기본 구조

별도의 형식이 필요한 문서가 아니라면 다음 구조를 기본으로 사용할 수 있다.

```markdown
# 문서 제목

## 1. 목적

## 2. 현재 상태 또는 적용 범위

## 3. 상세 내용

## 4. 오류·예외·경계 조건

## 5. 검증·인수 조건

## 6. 미결정·보류 항목
```

문서 성격상 필요 없는 절은 생략하거나 다른 구조를 사용할 수 있다.

형식을 맞추는 것보다 **실제 구현과 일치하고 이후 작업자가 이해할 수 있는 정보를 남기는 것**을 우선한다.

---

## 7. 구현 상태 관리

프로젝트에서 `IMPLEMENTATION_STATUS.md`를 사용하는 경우 구현 상태를 다음과 같이 구분한다.

- **계획** — 구현하기로 결정되었지만 아직 작업하지 않음
- **진행 중** — 현재 구현 작업 중
- **구현 완료** — 코드 구현은 완료됨
- **검증 완료** — 필요한 실행 또는 테스트까지 확인됨
- **미검증** — 구현되었으나 일부 또는 전체 검증이 이루어지지 않음
- **보류** — 결정 또는 외부 조건을 기다리는 상태

구현 완료와 검증 완료를 같은 의미로 사용하지 않는다.

프로젝트에 더 적합한 상태 체계가 존재하면 기존 방식을 따른다.

---

## 8. 테스트 및 검증 기록

프로젝트에서 `TEST_PLAN.md`를 사용하는 경우 다음 정보를 관리한다.

- 무엇을 검증해야 하는지
- 어떤 방식으로 검증하는지
- 실제 수행 여부
- 검증 결과
- 실패하거나 확인하지 못한 항목
- 추가 검증이 필요한 내용

검증하지 않은 기능을 검증 완료로 기록하지 않는다.

테스트 방식은 프로젝트와 변경 내용에 맞게 판단한다.

---

## 9. 미결정 사항

구현 과정에서 추가 결정이 필요한 내용이 발생하면 해당 문서의 **미결정·보류 항목**에 기록한다.

예:

```markdown
## 미결정·보류 항목

- 인증 토큰 만료 시간을 아직 확정하지 않음
- 운영 환경의 DB 구성 방식은 배포 구조 확정 후 결정
- 외부 API 장애 시 재시도 정책 추가 검토 필요
```

임시 판단으로 미결정 사항을 숨기지 않는다.

반대로 구현 과정에서 자연스럽게 결정할 수 있는 사소한 세부사항까지 모두 미결정 항목으로 남길 필요는 없다.

---

## 10. 문서와 구현의 관계

문서는 구현을 위한 참고 기준이지만, 구현 과정에서 더 적절한 구조나 방법이 발견될 수 있다.

기존 설계가 반드시 절대적인 제약이라고 가정하지 않는다.

필요한 경우 구현을 개선하고, 그 결과가 기존 문서와 달라졌다면 관련 문서를 실제 상태에 맞게 갱신한다.

다만 사용자 요구사항, 명확하게 확정된 제품 요구사항 또는 외부 시스템과의 계약 등 변경할 수 없는 조건은 유지한다.

---

## 11. 작업 완료 전 확인

작업을 완료하기 전에 관련되는 항목을 확인한다.

- 현재 구현과 관련 문서의 내용이 일치하는가?
- 새로 지속 관리해야 할 정보가 생겼는가?
- 신규 문서를 생성했다면 문서 목록에 등록했는가?
- 구현 상태가 실제 상태에 맞게 기록되어 있는가?
- 구현 완료와 검증 완료 또는 미검증 상태가 구분되어 있는가?
- 수행한 테스트나 검증 결과가 필요한 경우 기록되어 있는가?
- 미결정 사항을 확정된 내용처럼 기록하지 않았는가?
- 문서 링크가 실제 파일을 가리키는가?
- 문서나 예시에 비밀정보가 포함되어 있지 않은가?

모든 항목을 형식적으로 만족시키는 것이 목적은 아니다.

**프로젝트의 실제 상태를 이후 작업자가 정확히 파악할 수 있는 상태로 유지하는 것**을 최종 기준으로 한다.

---
